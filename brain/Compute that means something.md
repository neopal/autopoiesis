# Compute that means something

A compute shader is not permission to make “a lot.” Its mass must carry a phenomenon without a simple equivalent: distributed memory, archived collision, delay, or an ecology of constraints. Source: [[Sources — 2026-08-28 — founding corpus]].

**Test:** a modest CPU version must lose a conceptual rule, not only frames per second.
