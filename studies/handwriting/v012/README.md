# Handwriting v012 — The sentence enters a chorus.

## State

`v012` is a daily work candidate, recorded as **candidate / held**. It is not a finished period or an exhibition-ready claim.

## Hypothesis

If a remembered refusal makes several nearby routes occupy one finite phrase before they separate again, handwriting can show collective voice as a structural event rather than a caption or a decorative overlay.

## Changed rule

v011 made a refusal into a spatial stutter: one local family rehearsed a return beat. v012 replaces that rehearsal with a chorus. A local family approaches one displaced shared baseline, occupies the same compressed interval, then fans back out with lane-dependent exits and a carried offset. The line geometry, not the witness marks, owns the rule.

## Visible consequence

At rest, twenty-four imperfect horizontal passages cross a dark ruled field. When memory accumulates, neighbouring routes visibly draw inward, share one short phrase, and leave with altered spacing. Timeline choruses arrive at deterministic stages 02, 04, 06, 08, 10, 12, and 14. Pointer placement chooses the phrase position and affected family; Enter and Space repeat at the last pointer position. Lifting the latest chorus recomputes the exact preceding field.

## Deterministic state

- Seed: `0x57563132` (`mutine-handwriting-v012-chorus`)
- Renderer: deterministic Canvas 2D
- Stages: 17, final frame index 16
- Routes: 24, 56 points each, 18 nominal lanes
- Memory window: 6 choruses
- No external assets or image inputs

## Falsifier

Hide the corner labels, chorus witnesses, readout, captions, controls, and editorial furniture. If several routes do not still meet across a finite interval and visibly fan back out, the chorus is only a diagram. If the shared phrase is not legible at 320px-wide and 390px-wide viewports, this mutation fails its mobile test.

## Deletion condition

Delete v012 if a caption-free comparison finds parallel drift instead of a shared phrase, if the fan-out is only an accent overlay, if edge placement does not alter the route geometry, or if the copy explains more collective voice than the handwriting can show.

## Critique response

The structural critic asked for a finite shared phrase rather than another single-route hesitation; the engine derives one common chorus interval for the affected family and lets each route carry a distinct exit. The interaction critic asked for causal reversible memory; visitor input is replayed through the same route builder as timeline memory, and `removeLatestChorus` rebuilds from the preserved prior memory. The perceptual and cynical critiques remain release gates, not claims: this record stays **candidate / held** until the blind comparison and public readback are observed.
