# v003 response / browser evidence

## Work packet

- **Target:** typography / Handwriting
- **Date:** 2026-09-03
- **Title:** A sentence splits where it remembers.
- **Changed rule:** remembered refusals fracture later cadence into an asynchronous second hand.
- **Raw tableau:** `/studies/handwriting/v003/`
- **Canonical record:** `/works/typography-2026-09-03/`
- **Status:** candidate / held

## Evidence ledger

The deterministic engine test and the tableau contract test passed before the work was registered. The engine test compares the settled route field with a field rebuilt after one remembered scar is omitted; the later point coordinates differ and at least one route carries a branch.

A real local Chromium preview was exercised through the static and interactive paths. Exact viewport probes at `320×568`, `390×844`, `768×1024`, `1280×800`, and `1920×1080` returned the requested `innerWidth` / `innerHeight`; `scrollWidth` matched the layout `clientWidth` at every size, so there was no horizontal document overflow. The canonical page kept the iframe/tableau before the first explanatory heading, including at `320×568`, and the Handwriting timeline heading stayed in its own column at desktop width. Interactive DOM probing showed a `44px`-high action target; clicking `lift a scar` changed canvas pixels, and `restore sentence` returned the original pixels. A keyboard `Space` path on the focused canvas restored the memory state. Reduced motion settled directly at stage `7`. The raw tableau bridge resolved to `/works/typography-2026-09-03/`. Console exceptions and network loading failures were both `0` in the probe.

Static screenshots were visually inspected at `320×568`, `390×844`, `1280×800`, and `1920×1080`. The mobile field keeps both compact labels inside the viewport; interactive mobile controls sit in a bottom rail with 44px targets and do not cover the route field. The desktop composition holds a dark field of pale primary routes, green asynchronous branches, coral refusals, and faint historic strata without clipping.

## Critique held open

The work remains a candidate rather than a finished period. The accepted direction is to make memory structural: lifting a scar changes the actual later route and restoring it reproduces the prior field. The unresolved doubt is whether the green branch can remain visibly necessary rather than becoming a tasteful double stroke. The next mutation is a caption-free comparison with one remembered scar deleted; if the route does not read differently, delete or re-compose this direction.
