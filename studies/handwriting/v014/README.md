# Handwriting v014 — The sentence answers itself.

## State

`v014` is a daily work candidate, recorded as **candidate / held**. It is not a finished period or an exhibition-ready claim.

## Hypothesis

v013 let one route temporarily carry a neighbour's cadence. This version asks whether the borrowed motion can return as a structural answer: can handwriting show a route reflecting what it carried, rather than merely drifting away from it?

## Changed rule

A remembered cadence now creates a delayed counterphrase. A local family approaches one displaced phrase and occupies it together. At the next interval, each affected route temporarily inherits a neighbour's draft cadence. A few points later it reflects that borrowed deviation against its own draft, carries the counterphrase downstream, and settles with a changed exit. The answer is encoded in route coordinates and replayed from explicit memory; it is not a coloured band drawn over unchanged handwriting.

## Visible consequence

Twenty-four imperfect horizontal passages cross a dark ruled field. At each remembered return, a family compresses into a short phrase, passes a neighbour's rise and fall, then bends back against that borrowed cadence before leaving. The counterphrase should read as a delayed reply: not another marker at the first handoff, but a later reversal in the line itself. Timeline returns arrive at deterministic even stages; pointer placement chooses the phrase and family.

## Visitor action

In an interactive preview, touch or click the field, or use **return phrase**. Enter and Space repeat at the last pointer position. The latest return is inserted into memory and later route geometry is recomputed. **Lift latest** removes only the latest return and reconstructs the preceding route field; **release sequence** returns to the deterministic empty sequence. The focused canvas also supports `P` pause, `R` release, and `S` still export.

## Deterministic state

- Seed: `0x57563134` (`mutine-handwriting-v014-counterphrase`)
- Renderer: deterministic Canvas 2D
- Stages: 17, final frame index 16
- Routes: 24, 64 points each, 18 nominal lanes
- Memory window: 6 returns
- No external assets or image inputs

## Falsifier

Hide the counterphrase witnesses, labels, readout, captions, controls, and editorial furniture. If the lines do not still show approach → shared phrase → carried cadence → delayed answer → changed exit, the mutation is only a diagram. If the answer cannot be perceived at 320px-wide and 390px-wide viewports, this version fails its mobile test.

## Deletion condition

Delete v014 if a caption-free comparison finds only parallel drift, if the counterphrase is carried by an accent overlay instead of route coordinates, if edge placement has no structural consequence, or if lifting the latest event leaves the later route signature unchanged.

## Critique response

The structural critic asked for a consequence after the cadence relay rather than another convergence effect; the engine now reflects a neighbour's borrowed deviation later against each route's own draft. The interaction critic asked for a bounded, reversible field; visitor events are clamped, replayed through the same builder as timeline memory, and removed through the preserved prior memory. The perceptual and cynical critiques remain release gates, not claims: this record stays **candidate / held** until caption-free comparison, responsive browser evidence, and public readback are observed.
