# WebGPU v012 — The crowd keeps a hinge

## State

`v012` is the WebGPU daily candidate for 2026-09-17. It remains `candidate / held`: the deterministic engine and browser gates must pass, then an independent caption-free perceptual review must decide whether the hinge survives without its annotations.

## Creative hypothesis

A crowd becomes an archive when an absence changes the shape of arrival. The record should be a convergence, a fold in local order, and a fan that survives downstream—not a caption beside an unchanged crowd.

## Changed rule

v011 made a remembered absence temporal: mass held, released, and arrived late. v012 changes the rule: a remembered absence pulls a cohort toward a narrow hinge, folds its local vertical order across that hinge, and fans the cohort back out with a displaced downstream trace. The memory window retains eight hinge records. Visitor coordinates are clamped to the inner field so edge gestures still create a real event.

## Visible consequence

Twenty-one deterministic states carry 540 agents through a blue-black field as signal, body, and dust strata. The agent coordinates contain distinct convergence, fold, fan, and trace influences. Annotated mode draws the five-point hinge witness; blind mode removes labels, readouts, controls, and witnesses so the field must carry the proposition itself.

## Visitor action

In an interactive preview, click the field or focus it and press `Enter`/`Space` to make a bounded hinge. `Delete`/`Backspace` or **lift latest** removes the newest record and rebuilds the crowd. **release current** clears visitor state and restarts the seeded sequence. Pointer and keyboard use the same engine path; neither is a marker-only toggle.

## Determinism

The fixed seed is `0x5747503c` in `engine.mjs`. The same stage and memory return the same agents, route records, hinge ledger, and aggregate values. No browser history or local storage is used.

## Falsifier and deletion condition

Delete this direction if a caption-free viewer cannot identify convergence, a fold, and a downstream fan when hinge witnesses, labels, and readouts are hidden. Delete it if the effect is only a glow, if the cohort does not change local order in actual coordinates, if edge clicks produce no bounded hinge, or if lifting the latest record does not restore the exact prior field.

## Evidence boundary

Browser, release, and perceptual evidence are recorded separately under `research/qa/proofs/webgpu-v012-2026-09-17/`. The work remains held until those observations are complete; a local record or scheduler slot is not evidence that a deployment happened.
