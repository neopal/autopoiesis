# WebGPU v008 — The crowd keeps an echo

## State

`v008` is the 2026-09-12 WebGPU daily candidate. It remains `candidate / held` for an independent caption-free perceptual review; the deterministic engine and browser contract are the evidence gathered in this run.

## Creative hypothesis

A crowd becomes an archive when an absence is not filled in, but relayed late. The record should be a displaced second passage inside the mass, not a label placed beside it.

## Changed rule

v007 made a remembered absence split opposing routes and compress the returning bodies into a shared wake. v008 changes the rule: a remembered absence opens the lanes, delays a relay across alternating strata, and leaves a phase-shifted echo downstream. The finite memory window retains five echo records. Near the right edge, the available runway is shortened so a bounded visitor echo still produces a visible relay rather than falling off the field.

## Visible consequence

Sixteen deterministic states carry 480 agents through a graphite field as signal, body, and dust strata. The underlying agent coordinates open, exchange a delayed relay, and then carry a displaced second passage whose offset is measurable in the final frame. The echo remains in the rows as a changed coordinate and a repeat contour; it is not only a glow, counter, or witness path.

## Visitor action

In an interactive preview, click the field or focus it and press `Enter`/`Space` to leave a bounded echo. `Delete`/`Backspace` or **lift latest** removes the newest record and rebuilds the crowd. **release relay** clears visitor state and restarts the seeded sequence. Pointer and keyboard use the same engine path; neither is a marker-only toggle.

## Determinism

The fixed seed is `0x57475038` in `engine.mjs`. The same stage and memory return the same agents, route records, echo ledger, and aggregate values. No browser history or local storage is used.

## Falsifier and deletion condition

Delete this direction if a caption-free viewer cannot identify a delayed second passage in the crowd strata when echo witnesses, labels, and readouts are hidden. Delete it if the echo is only a glow, if the relay never changes downstream coordinates, if edge clicks produce no echo, or if lifting the latest record does not restore the exact prior field.

## Evidence boundary

The local browser evidence is recorded in `research/qa/proofs/webgpu-v008/results.json`, with an independent production readback in `production-results.json` when available. The work remains held until the local matrix, interaction readback, deployment readback, and an independent caption-free perceptual comparison all pass.
