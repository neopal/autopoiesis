# WebGPU v009 — The crowd keeps a countercurrent

## State

`v009` is the 2026-09-13 WebGPU daily candidate. It remains `candidate / held` until the local browser matrix, deployment readback, and an independent caption-free perceptual review are complete.

## Creative hypothesis

A crowd becomes an archive when an absence can move against its own passage. The record should be a return-current inside the mass, not a label placed beside it.

## Changed rule

v008 made a remembered absence delay a relay and leave a second passage downstream. v009 changes the rule: a remembered absence reverses a bounded packet against the main flow, bends it at a turning seam, then pushes it back into the crowd at a later downstream exit. The finite memory window retains six countercurrent records. Near the right edge, the entry and turning coordinates remain bounded so a visitor action still produces a real reversal instead of falling off the field.

## Visible consequence

Seventeen deterministic states carry 480 agents through a dark teal field as signal, body, and dust strata. The underlying agent coordinates contain a negative x displacement during the reversal window and a later rejoin offset. In the final seeded state, the engine measures 253 counterflow agents, 198 reverse agents, 213 rejoined agents, countercurrent strength `0.329990931876258`, and a latest-record field delta of `4.860632377932714` normalized units against its lifted reconstruction.

## Visitor action

In an interactive preview, click the field or focus it and press `Enter`/`Space` to mark a bounded countercurrent. `Delete`/`Backspace` or **lift latest** removes the newest record and rebuilds the crowd. **release current** clears visitor state and restarts the seeded sequence. Pointer and keyboard use the same engine path; neither is a marker-only toggle.

## Determinism

The fixed seed is `0x57475039` in `engine.mjs`. The same stage and memory return the same agents, route records, countercurrent ledger, and aggregate values. No browser history or local storage is used.

## Falsifier and deletion condition

Delete this direction if a caption-free viewer cannot identify a packet travelling against the main flow and then rejoining downstream when route witnesses, labels, and readouts are hidden. Delete it if the reversal is only a glow, if the turn does not alter agent coordinates, if edge clicks produce no bounded return-current, or if lifting the latest record does not restore the exact prior field.

## Evidence boundary

Browser, release, and perceptual evidence are recorded separately under `research/qa/proofs/webgpu-v009/`. The work remains held until those observations are complete; a local record or scheduler slot is not evidence that a deployment happened.
