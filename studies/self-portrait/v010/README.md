# Self portrait / v010

## Status

**Candidate / held.** This is the 2026-09-15 daily tableau for the Self portrait current. It remains held until an independent caption-free perceptual comparison confirms that the fork is visible as a bodily choice rather than two lines over an unchanged face.

## Creative record

- **Hypothesis:** a self-image can expose its decisions by holding alternatives in the same body, rather than collapsing every choice into one scar or one gaze.
- **Changed rule:** a remembered decision becomes a fork. One trunk leaves the aperture, splits into two materially different lanes, reunites at a shared merge, and continues toward a changed lower contour. The aperture follows the accumulated branch pressure.
- **Visible consequence:** the portrait carries an actual one-to-two-to-one route grammar: common origin, separated waypoints, common merge, and downstream resolution. A visitor gesture changes those routes, contour geometry, aperture geometry, and lane separation, not only colour or text.
- **Deterministic state:** `SEED = 0x53505640`; `STAGES = 16`; `MEMORY_LIMIT = 4`; `PRIMITIVE_BUDGET = 62`; 72 contour points, 15 trunk points, 21 points per branch, and 13 resolution points. The engine is pure and replayable.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `split a route`. `return latest fork` rebuilds the exact preceding geometry; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if returning the latest fork leaves the counter-contour, aperture, split, merge, branch separation, or resolution unchanged, the direction is decorative and fails.
- **Deletion condition:** delete v010 if a caption-free pair cannot identify one trunk becoming two lanes and then resolving, or if the route lines alone carry the meaning over a stable face.

## Lineage

`portrait-2026-09-14` → v010: from a seam that crossed the body to a fork that makes the body hold two possible routes before resolving them.

## Evidence boundary

The focused unit tests prove deterministic bounded input, changed contour and aperture geometry, shared split/merge route structure, separated branch lanes, finite memory, and exact restoration. Browser and independent caption-free perceptual evidence must be recorded separately; until those gates are observed, this work stays held.
