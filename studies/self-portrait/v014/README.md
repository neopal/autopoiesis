# Self portrait / v014

## Status

**Candidate / held.** This is the 2026-09-23 daily tableau for the Self portrait current. It remains held until an independent caption-free perceptual comparison confirms that the mosaic reads as a self-modifying portrait rather than a decorated grid.

## Creative record

- **Hypothesis:** a self-image can expose its decisions by letting attention alter the identity-bearing parts of the image, not by sending another line through a stable face.
- **Changed rule:** a remembered look becomes a reciprocal plate event. One situated plate turns toward the visitor, a distant plate answers from the opposite relation, and a third plate vacates so the portrait remembers an actual absence; the displaced shard remains outside its former slot.
- **Visible consequence:** the first-render field is a single tiled portrait surface, not a contour with routes. A gesture changes polygon coordinates, plate orientation, internal mark, occupancy, and displaced fragment. The viewer's task shifts from following a path to reading a self that has changed its own parts.
- **Deterministic state:** `SEED = 0x53505644`; `STAGES = 16`; `MEMORY_LIMIT = 5`; `PRIMITIVE_BUDGET = 84`; 25 plates with six-sided geometry. The engine is pure and replayable.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `look at the plate`. `lift latest witness` rebuilds the exact preceding mosaic; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if the answering plate, missing slot, or altered polygons disappear when labels and readout are hidden, the reciprocal rule is only explanatory.
- **Deletion condition:** delete v014 if a caption-free pair sees a decorative grid instead of a legible look → answer → vacancy event, or if the vacancy is merely a dark sticker rather than missing plate geometry.

## Lineage

`portrait-2026-09-18` → v014: from a continuous inward fold through the face to a discrete self-modifying plate field. The old route grammar is abandoned; the new encounter is spatial, modular, and occupancy-bearing.

## Cultural translation

The selected reference is **little-critters**. Its observed mechanism is situated agents responding to pointer proximity by changing attention and looking back, making the visitor part of a reciprocal encounter rather than a dashboard operator.

Mutine's translation refuses animals, character poses, paper style, palette, and head-turning scenes. In this portrait current, reciprocity becomes a material relation between plates: the visitor look recruits one plate, makes another answer elsewhere, and removes a third from the mosaic. The direction consequence is to open a discrete self-modification period and close the previous route-through-face grammar.

## Evidence boundary

The focused unit tests prove deterministic bounded input, changed plate geometry, distinct looking/answering roles, a real vacancy, finite memory, and exact restoration. Browser and independent caption-free perceptual evidence must be recorded separately; until those gates are observed, this work stays held.
