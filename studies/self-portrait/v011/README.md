# Self portrait / v011

## Status

**Candidate / held.** This is the 2026-09-16 daily tableau for the Self portrait current. It remains held until an independent caption-free perceptual comparison confirms that the exchange is visible as a bodily decision rather than two coloured lines over an unchanged face.

## Creative record

- **Hypothesis:** a self-image can expose its decisions by carrying alternatives through a change of order, not merely by splitting them apart.
- **Changed rule:** a remembered fork becomes a braid. One trunk leaves the aperture, divides into two lanes, exchanges their lateral order at a bounded middle crossing, reunites at a shared merge, and continues toward a changed contour. The aperture follows accumulated braid pressure.
- **Visible consequence:** the portrait carries an actual common-origin → two-lanes → lane-exchange → common-merge → downstream-resolution grammar. A visitor gesture changes those routes, contour geometry, aperture geometry, and lane order, not only colour or text.
- **Deterministic state:** `SEED = 0x53505641`; `STAGES = 17`; `MEMORY_LIMIT = 4`; `PRIMITIVE_BUDGET = 68`; 80 contour points, 13 trunk points, 25 points per lane, and 13 resolution points. The engine is pure and replayable.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `cross two answers`. `return latest braid` rebuilds the exact preceding geometry; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if returning the latest braid leaves the counter-contour, aperture, exchange, merge, or resolution unchanged, the direction is decorative and fails.
- **Deletion condition:** delete v011 if a caption-free pair cannot identify lanes that actually exchange order, or if the braid is only two parallel decorations over a stable face.

## Lineage

`portrait-2026-09-15` → v011: from a fork that held two possibilities to a braid that makes the possibilities exchange places before resolving.

## Evidence boundary

The focused unit tests prove deterministic bounded input, changed contour and aperture geometry, one trunk, separated lanes, explicit exchange metadata, finite memory, and exact restoration. Browser and independent caption-free perceptual evidence must be recorded separately; until those gates are observed, this work stays held.
