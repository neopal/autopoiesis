# Self portrait / v012

## Status

**Candidate / held.** This is the 2026-09-17 daily tableau for the Self portrait current. It remains held until an independent caption-free perceptual comparison confirms that the passage reads as a bodily decision rather than an ornamental line over an unchanged face.

## Creative record

- **Hypothesis:** a self-image can expose its decisions by making a limit traversable, not by merely splitting or exchanging alternatives.
- **Changed rule:** a remembered decision becomes a threshold crossing. A route approaches a bounded boundary, skims its edge, crosses through it once, enters the interior, and departs through a changed contour anchor. The aperture follows accumulated crossing pressure.
- **Visible consequence:** the portrait carries an actual approach → boundary → crossing → interior → downstream departure grammar. A visitor gesture changes those route coordinates, contour geometry, aperture geometry, and exit, not only colour or text.
- **Deterministic state:** `SEED = 0x53505642`; `STAGES = 18`; `MEMORY_LIMIT = 4`; `PRIMITIVE_BUDGET = 72`; 84 contour points and five sampled route sections. The engine is pure and replayable.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `cross the threshold`. `return latest threshold` rebuilds the exact preceding geometry; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if returning the latest threshold leaves the counter-contour, aperture, crossing distance, or downstream departure unchanged, the direction is decorative and fails.
- **Deletion condition:** delete v012 if a caption-free pair cannot identify an actual boundary crossing followed by an interior passage, or if the threshold is only a witness mark over a stable face.

## Lineage

`portrait-2026-09-16` → v012: from a braid that exchanged two possible answers to a threshold that makes the act of crossing itself carry downstream consequence.

## Evidence boundary

The focused unit tests prove deterministic bounded input, changed contour and aperture geometry, an explicit boundary-crossing route, finite memory, and exact restoration. Browser and independent caption-free perceptual evidence must be recorded separately; until those gates are observed, this work stays held.
