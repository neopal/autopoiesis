# Self portrait / v004

## Status

**Candidate / held.** This is the 2026-09-08 daily tableau for the Self portrait current. It remains held until a caption-free perceptual comparison confirms that the split and displaced rejoin carry the decision without the readout or route witnesses.

## Creative record

- **Hypothesis:** a self-image can expose its decisions through a route that refuses to stay singular, rather than through a supplied face.
- **Changed rule:** a remembered refusal becomes a fork. Later body routes divide into two near-contours around one interior aperture and recombine at a displaced shoulder; the contour, axis, and aperture are rebuilt from the same memory.
- **Visible consequence:** the body no longer only leans around an absence. It opens a pair of materially different routes that share a split and rejoin point, so the memory changes the path back into one body.
- **Deterministic state:** `SEED = 0x53505634`; `STAGES = 10`; `MEMORY_LIMIT = 4`; `PRIMITIVE_BUDGET = 44`; 36 contour points and 11 points per fork route. The engine is pure and replayable.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `withhold a place`. `lift latest refusal` rebuilds the exact preceding geometry; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if removing the latest refusal leaves the fork, rejoin, contour, or aperture unchanged, the direction is decorative and fails.
- **Deletion condition:** delete v004 if a caption-free pair cannot identify the same split and displaced shoulder, or if the aperture alone carries the meaning.

## Lineage

`portrait-2026-09-04` → v004: from an interior negative aperture to a route that divides around it and recombines elsewhere.

## Evidence boundary

The focused unit tests prove deterministic bounded input, a geometry delta, a visible fork route, and exact restoration. Browser and independent caption-free perceptual evidence must be recorded separately; until those gates are observed, this work stays held.
