# Self portrait / v008

## Status

**Candidate / held.** This is the 2026-09-12 daily tableau for the Self portrait current. It remains held until a caption-free perceptual comparison confirms that the routed absence, split lips, and downstream return carry the decision without labels or witnesses.

## Creative record

- **Hypothesis:** a self-image can expose its decisions by routing around what it will not retain, rather than by printing a second gaze or folding its outline.
- **Changed rule:** a remembered decision becomes a blind spot. A local contour span is displaced into two lips that separate around the absence and rejoin downstream; the aperture shifts as if the face has to keep living around the missing place.
- **Visible consequence:** the portrait carries an actual split-and-rejoin event in its contour and a return route from the absent center. A visitor gesture changes those routes, contour geometry, and aperture geometry, not only colour or text.
- **Deterministic state:** `SEED = 0x53505638`; `STAGES = 14`; `MEMORY_LIMIT = 4`; `PRIMITIVE_BUDGET = 50`; 56 contour points, 13 points per lip, and 16 points per return route. The engine is pure and replayable.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `keep a blind spot`. `return latest absence` rebuilds the exact preceding geometry; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if returning the latest absence leaves the counter-contour, aperture, split lips, or downstream route unchanged, the direction is decorative and fails.
- **Deletion condition:** delete v008 if a caption-free pair cannot identify a local absence with two contour edges that reunite, or if the dark spot / return line alone carries the meaning.

## Lineage

`portrait-2026-09-11` → v008: from a counter-gaze that looks away to a blind spot that the contour must route around.

## Evidence boundary

The focused unit tests prove deterministic bounded input, a changed contour, separated and rejoined routes, a displaced aperture, and exact restoration. Browser and independent caption-free perceptual evidence must be recorded separately; until those gates are observed, this work stays held.
