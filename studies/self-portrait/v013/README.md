# Self portrait / v013

## Status

**Candidate / held.** This is the 2026-09-18 daily tableau for the Self portrait current. It remains held until an independent caption-free perceptual comparison confirms that the inward fold reads as a bodily decision rather than an ornamental crossing over an unchanged face.

## Creative record

- **Hypothesis:** a self-image can expose its decisions by showing attention change the shape of the gaze, not by merely adding an expressive mark.
- **Changed rule:** a remembered attention becomes an inward fold. A route approaches the gaze, compresses toward a hinge, folds across it, releases through the interior, and departs from a changed contour anchor. The aperture follows accumulated attention pressure.
- **Visible consequence:** the portrait carries an actual approach → compression → fold → release → downstream departure grammar. A visitor gesture changes those route coordinates, contour geometry, aperture geometry, and exit, not only colour or text.
- **Deterministic state:** `SEED = 0x53505643`; `STAGES = 18`; `MEMORY_LIMIT = 4`; `PRIMITIVE_BUDGET = 78`; 96 contour points and five sampled route sections. The engine is pure and replayable.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `turn inward`. `lift latest fold` rebuilds the exact preceding geometry; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if lifting the latest fold leaves the counter-contour, aperture, hinge span, or downstream departure unchanged, the direction is decorative and fails.
- **Deletion condition:** delete v013 if a caption-free pair cannot identify an actual compression and fold followed by a released gaze, or if the fold is only a witness mark over a stable face.

## Lineage

`portrait-2026-09-17` → v013: from a threshold that crossed the face to an attention that narrows, folds inward, and leaves its turn in the later gaze.

## Evidence boundary

The focused unit tests prove deterministic bounded input, changed contour and aperture geometry, an explicit attention-fold route, finite memory, and exact restoration. Browser and independent caption-free perceptual evidence must be recorded separately; until those gates are observed, this work stays held.
