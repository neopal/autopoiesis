# Self portrait v002 — The axis keeps its hesitation

## State

`v002` is the 2026-09-03 daily work for the Self portrait current. It is an active candidate held for an independent perceptual critique; it is not a finished period.

## Creative hypothesis

A system can portray a decision without supplying a face if its refusals alter the shape that follows. The portrait is a warm, translucent contour under a bent axis: an abstract body of decisions, not a likeness.

## Changed rule

v001 pushed later contour points gently away from stored blind spots. v002 turns each refusal into a hinge. Later contour mass is displaced by the remembered hinge and the internal axis accumulates a signed lean. The hinge line is generated from the same memory that builds the silhouette; it is not a static annotation layer.

## Visible consequence

Eight deterministic states move from a proposal contour to a kept contour. At the settled state, four hinges remain in a bounded memory. The same final stage without the latest hinge changes the contour by a maximum normalized distance of `0.04770150801286075` (`54.14121159459695px` at a 1135px field), and changes the axis wobble. A caption-free comparison is still the art gate: the orange hinge witnesses cannot carry the claim alone.

## Visitor action

In an interactive preview, click the field or focus it and press `Enter`/`Space` to lean the axis at a new point. `lift latest hinge` rebuilds the prior geometry for comparison. `release sequence` returns to the passive timeline. The action changes contour geometry, axis tilt, and memory; it does not merely toggle visibility.

## Determinism

- seed: `0x53505632`
- stages: `8`
- memory: bounded to four hinges
- no local storage or browser history
- reduced motion settles on the final state

## Falsifier and deletion condition

Delete this direction if hiding the hinge witnesses leaves no identifiable downstream bend, or if removing the latest hinge does not change a later contour segment and the axis. Also delete it if the silhouette reads as a generic human avatar rather than abstract decision geometry.

## Evidence boundary

The source packet records the authored rule. Browser runtime, interaction, reduced-motion, responsive, console, network, and visual observations are recorded separately in `response.md` and `research/qa/proofs/portrait-v002-2026-09-03/results.json`. The work remains held where evidence is unresolved.
