# Pure SVG / v010

## Status

**Candidate / held.** This is a real daily tableau, not a placeholder. It is the tenth mutation of the Pure SVG current and stays held until an independent caption-free comparison confirms that the lean and compensating gait survive without their witness marks.

## Creative record

- **Hypothesis:** a refusal can become a counterweight inside an animal instead of opening another pocket.
- **Changed rule:** each remembered refusal stores a contour anchor and a later balance seam. Between them, the body leans around an inner spine; after the seam, the displacement is carried forward and the three feet resolve it as loaded or countered postures.
- **Visible consequence:** one animal keeps a family resemblance while its downstream silhouette tilts, its spine bends through the weighted segment, and its feet answer with different route geometry. Gold witness circles, vector threads, labels, and the readout are witnesses only; the contour and gait carry the rule.
- **Deterministic state:** `SEED = 0x53564740`; `STAGES = 13`; `PRIMITIVE_BUDGET = 19`; memory is capped to four counterweights. Pointer input is bounded to `x: 0.06..0.94`, `y: 0.16..0.84`.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `place a weight`. `lift latest` recomputes the preceding body and routes; `release the body` returns to the autonomous sequence.
- **Falsifier:** hide the weight points, vector threads, labels, and readout. If the contour does not visibly lean through a seam and the feet do not retain its load/counter-load consequence, the direction fails.
- **Deletion condition:** delete v010 if lifting the latest weight leaves downstream contour or route coordinates unchanged, or if the animal reads as a stable body with decorative balance marks.

## Lineage

`pure-svg-v009` → v010: from an inward negative-space pocket to a distributed counterweight that changes body balance and gait.

## Evidence state

The deterministic engine, bounded visitor input, exact undo, tableau-first contract, responsive CSS hooks, local headless browser matrix, and production readback are recorded in `metrics.json` and the dated QA proof. The independent caption-free perceptual review and provider revision remain held boundaries; a passing runtime check is not a perceptual approval.
