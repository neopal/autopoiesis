# Pure SVG / v004

## Status

**Candidate / held.** This is a real daily tableau, not a placeholder. It is the fourth mutation of the Pure SVG current and remains held until an independent caption-free perceptual comparison confirms that the negative anatomy carries the decision without its witnesses.

## Creative record

- **Hypothesis:** a shape can remember a refusal without keeping a wound; memory can become a missing chamber inside the body.
- **Changed rule:** every remembered refusal contributes to a compound SVG silhouette with an interior aperture. Later limb joints, knees, and feet inherit avoidance pressure from that chamber instead of receiving only a topological detour or a painted scar.
- **Visible consequence:** the mineral body is hollowed by a copper-rimmed negative chamber and the three possible routes approach around its missing interior. The chamber drifts and grows with inherited refusals while the body stays restrained and strange.
- **Deterministic state:** `SEED = 0x53564734`; `STAGES = 10`; `PRIMITIVE_BUDGET = 18`; memory is capped to the five most recent refusals. Pointer input is bounded to `x: 0.04..0.96`, `y: 0.12..0.88`.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use the named `open a chamber` button. `close latest chamber` recomputes the preceding body and routes; `let it continue` returns to the autonomous sequence.
- **Falsifier:** if a caption-free viewer sees only an orange rim or witnesses and not a changed aperture and body weight, the direction fails.
- **Deletion condition:** delete this version if removing the latest refusal leaves the aperture or downstream joint/knee/foot routes unchanged, or if the negative chamber reads as a decorative eye rather than structural absence.

## Lineage

`pure-svg-v003` → v004: from a refused joint that changed attachment topology to a compound body that carries refusal as negative anatomy.

## Evidence state

The deterministic engine and interaction contract are covered by `tests/pure-svg-v004.test.mjs`. A fresh Hermes headless local run observed the canonical work at `http://localhost:4173/works/svg-2026-09-04/`: parent width `1264px`, parent `scrollWidth` `1249px`, iframe `714.28125×720`, and frame `scrollWidth` `714px`. The initial probe rendered the aperture rim and three detour routes with an empty console buffer. Opening a chamber changed the visible routes; closing the latest chamber showed `REFUSAL REMOVED / PAUSED` and rebuilt the prior visible state; clicking into the iframe and pressing Enter showed `VISITOR REFUSAL / PAUSED`. Direct raw navigation resolved to the canonical work route, and `?preview=1` showed the tableau without editorial furniture.

The required `320×568`, `390×844`, `768×1024`, `1280×800`, and `1920×1080` matrix, an independent network-failure log, and an independent caption-free perceptual review remain unresolved; this is why the candidate stays held.
