# Pure SVG / v008

## Status

**Candidate / held.** This is a real daily tableau, not a placeholder. It is the eighth mutation of the Pure SVG current and stays held until an independent caption-free perceptual comparison confirms that the handoff survives without its witness marks.

## Creative record

- **Hypothesis:** a refusal can be passed onward through an animal instead of remaining as an echo, hinge, or weight.
- **Changed rule:** every remembered refusal stores a contour anchor and a later receiver. Downstream contour points alternate around that handoff; later legs inherit the same lead/return order.
- **Visible consequence:** one animal keeps its body family while its later outline switches sides around a receiver and its three feet land in a shared alternating relay. Knot circles and handoff threads are witnesses only; the body and gait carry the rule.
- **Deterministic state:** `SEED = 0x53564738`; `STAGES = 12`; `PRIMITIVE_BUDGET = 18`; memory is capped to four relays. Pointer input is bounded to `x: 0.04..0.96`, `y: 0.12..0.88`.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `pass on a refusal`. `unlink latest` recomputes the preceding body and routes; `release the body` returns to the autonomous sequence.
- **Falsifier:** hide the relay points, handoff threads, labels, and readout. If the contour does not visibly alternate around a later receiver and the feet do not share its lead/return order, the direction fails.
- **Deletion condition:** delete this version if unlinking the latest relay leaves downstream contour points or route coordinates unchanged, or if the animal reads as a static body with decorative signals.

## Lineage

`pure-svg-v007` → v008: from a delayed phase echo to a relay knot that passes its displacement into a later receiver and a shared alternating gait.

## Evidence state

The deterministic engine, bounded visitor input, exact undo, tableau-first contract, responsive CSS hooks, and daily register are covered by `tests/pure-svg-v008.test.mjs`. Browser evidence and the independent caption-free review are intentionally held until a real headless browser run supplies them.
