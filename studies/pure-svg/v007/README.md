# Pure SVG / v007

## Status

**Candidate / held.** This is a real daily tableau, not a placeholder. It is the seventh mutation of the Pure SVG current and stays held until an independent caption-free perceptual comparison confirms that the delayed rhythm survives without its witness marks.

## Creative record

- **Hypothesis:** a refusal can remain in the animal as a delayed echo rather than a wound, hinge, or weight.
- **Changed rule:** every remembered refusal stores an earlier contour point and a lag. Downstream contour points borrow that earlier rhythm; later legs inherit the same delayed landing.
- **Visible consequence:** one animal keeps its recognisable body family while its rear contour and feet arrive one step late. Cyan echo marks and threads are witnesses only; the body and gait carry the rule.
- **Deterministic state:** `SEED = 0x53564737`; `STAGES = 11`; `PRIMITIVE_BUDGET = 17`; memory is capped to four afterimages. Pointer input is bounded to `x: 0.04..0.96`, `y: 0.12..0.88`.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `place an afterimage`. `erase latest` recomputes the preceding body and routes; `release the body` returns to the autonomous sequence.
- **Falsifier:** hide the afterimage points, echo threads, labels, and readout. If the contour does not visibly repeat an earlier rhythm and the feet do not share its delay, the direction fails.
- **Deletion condition:** delete this version if erasing the latest afterimage leaves downstream contour points or route coordinates unchanged, or if the animal reads as a static body with decorative signals.

## Lineage

`pure-svg-v006` → v007: from remembered counterweight and displaced footing to a phase echo that makes the body and gait arrive late.

## Evidence state

The deterministic engine, bounded visitor input, exact undo, tableau-first contract, responsive CSS hooks, and daily register are covered by `tests/pure-svg-v007.test.mjs`. Browser evidence and the independent caption-free review are intentionally marked held in `metrics.json`; no visual or deployment claim is made until a real headless browser run supplies it.
