# Brush / v003 — A brush opens a channel.

## Status

**Candidate / held.** This is a real daily tableau for the Brush current, not a placeholder. It remains held until a caption-free perceptual comparison confirms that the rerouted stroke carries the decision without its witness marks.

## Creative record

- **Hypothesis:** a brush can remember loss as a passage through which later matter has to negotiate.
- **Changed rule:** v002's autonomous wound becomes a dry channel with an exit and re-entry. Later wet strokes bend away from the opening, then return displaced; the channel is an input to geometry, not a mark over it.
- **Visible consequence:** warm wet strokes leave their lane in local, opposing bends around remembered cuts. The route re-enters with a lingering offset, so memory changes the stroke's body rather than only its colour.
- **Deterministic state:** `SEED = 0x42525533`; `STAGES = 8`; five strokes per stage; `MEMORY_LIMIT = 6`; `PRIMITIVE_BUDGET = 30`. The final reduced-motion/static state is the last timeline frame.
- **Interaction:** click or tap the field, press Enter/Space after focusing it, or use `open a cut`; `lift latest` rebuilds the preceding geometry; `release sequence` returns to the autonomous timeline.
- **Falsifier:** if a later stroke is unchanged by an empty-memory replay or the latest cut's removal, the channel is only decoration.
- **Deletion condition:** delete the dry-channel rule if the witness trace carries more meaning than the rerouted wet stroke in a caption-free comparison.

## Lineage

`brush-2026-08-31` → v003: from autonomous wounds returning to the field to retained cuts becoming channels that redirect subsequent material.

## Release note

The candidate is intentionally held. The independent review must compare the same settled frame with channel witnesses hidden and restored, at the five required viewport sizes before promotion.
