{
  "renderer": "deterministic Canvas 2D",
  "seed": "0x4e413039",
  "stages": 14,
  "primitiveBudget": 44,
  "memoryWindow": 6,
  "visitorInput": true,
  "replay": true,
  "changedRule": "a retained correction becomes an approach-threshold-inside-exit doorway replayed from house, ground, and route origins",
  "memoryRule": "later scenes keep a shared permission to pass: each retained mistake crosses a threshold, travels inside, and exits downstream",
  "interactionRule": "visitor placement changes doorway side, width, depth, tilt, and all three route geometries; undo restores the exact prior field",
  "structuralWitnesses": ["house-doorway", "ground-door-line", "route-doorway"],
  "settledStage": 14,
  "settledMemory": 6,
  "settledApproachPoints": 18,
  "settledThresholdPoints": 18,
  "settledInsidePoints": 18,
  "settledExitPoints": 18,
  "browserEvidence": {
    "status": "local headless browser matrix passed / held for independent caption-free perceptual review",
    "observedRoute": "http://127.0.0.1:4173/works/naive-2026-09-13/",
    "rawPreview": "http://127.0.0.1:4173/studies/naive-art/v009/?preview=1&interaction=1",
    "canonicalShellAndEmbeddedTableauRendered": true,
    "tableauFirst": "all ten local canonical runs rendered the v009 tableau iframe before generated title, Journal, critique, evidence, and neighbouring-work prose",
    "viewportMatrix": "10/10 local headless runs passed at 320x568, 390x844, 768x1024, 1280x800, and 1920x1080 in normal and reduced-motion modes",
    "overflow": "outer innerWidth equaled clientWidth and scrollWidth at all ten local canonical runs; the only larger scrollWidth was the contained horizontal daily timeline rail; embedded study frames matched their own geometry",
    "touchTargets": "all three local raw interaction buttons measured 44 CSS px at 390x844",
    "initialConsoleProbe": "zero console messages, page errors, failed requests, and HTTP 400+ responses across all ten local canonical, raw interaction, and static blind runs",
    "networkProbe": "zero failed requests and zero HTTP 400+ responses across all ten local canonical, raw interaction, and static blind runs",
    "normalState": "normal-motion canonical runs opened at stage 01 / 14 with 0 doorways retained",
    "reducedMotionState": "reduced-motion canonical and static blind runs settled at stage 14 / 14 with 6 doorways retained",
    "pointerInteraction": "local raw pointer changed memory from 0 to 1 doorway and paused the tableau; canvas pixels changed",
    "keyboardInteraction": "Enter on the focused canvas changed memory from 1 to 2 doorways and changed the route field",
    "liftInteraction": "undo latest changed memory from 2 to 1 and restored the exact preceding pointer-state canvas PNG data URL",
    "releaseInteraction": "second undo returned memory to 0 and release sequence returned to stage 01 / 14 with 0 doorways",
    "staticBlindPreview": "reduced-motion static blind preview at 390x844 kept the canvas visible while hiding editorial furniture, controls, readout, labels, and doorway notation; no overflow",
    "canvasCaptures": "12 PNG captures written: ten local canonical viewport/motion captures, one raw interaction capture, and one static blind capture",
    "evidenceFiles": "research/qa/proofs/naive-art-v009-2026-09-13/results.json, probe.mjs, and twelve PNG captures",
    "unresolved": [
      "independent caption-free perceptual comparison with doorway dots, labels, bridge, readout, and editorial furniture hidden",
      "stable-alias production readback and provider revision linking"
    ]
  }
}
