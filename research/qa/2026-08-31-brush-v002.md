# Brush v002 — local exhibition QA

- Date: 2026-08-31
- Work: `/chantiers/p5-brush/v002/`
- Status: `candidate / held after independent perceptual critique`
- Local server: `127.0.0.1:4174`

## Decision

Brush v002 is integrated as the latest autonomous candidate in the Brush current. Brush v001 remains a separately classified visitor-operated interaction. v002 is not promoted to a validated artwork: the independent perceptual review returned `HOLD`.

## Rule under test

The brush paints four strokes, performs an autonomous return cut, retains that wound, and bends later strokes around the inherited wound. The return is authored by the timeline, not initiated by a visitor.

## Engine evidence

The deterministic probe returned:

```json
{
  "stages": 8,
  "memoryAtStage5": 4,
  "changedAgainstEmpty": 21,
  "changedAfterDeletion": 19,
  "replay": true
}
```

The comparison is geometric: later stroke points are compared against the same stage with empty memory and with the latest inherited wound deleted. This supports a structural consequence, but does not by itself prove that the consequence reads artistically.

## Revision after independent HOLD

The first perceptual review found the wound visible but the wound-to-detour relation too diffuse. The engine now calculates a localized detour envelope around each inherited wound's position along each later stroke, while retaining the broader accumulated turn.

The post-revision probe measured `maxLocalDetour = 0.080828` at stage 05 against the same stage with empty memory, with deterministic replay still true across all eight stages. A fresh independent perceptual review now returned `KEEP`: the reduced compositions show localized V/S-shaped contour detours beside wound clusters while the normal row remains comparatively smooth. The candidate is perceptually kept locally, but the release gate remains open until the deployed public URL is verified.

## Browser evidence

Observed on the local direct route:

- URL: `http://127.0.0.1:4174/chantiers/p5-brush/v002/`
- viewport reported by the review browser: `1264×625`
- `scrollWidth = clientWidth = 1249`
- Canvas `toDataURL()` length: `152918`
- controls: `0`
- passive stage readout observed changing through the timeline
- no pointer, keyboard, click, input, select, or button control is present

Observed on the isolated preview:

- URL: `http://127.0.0.1:4174/chantiers/p5-brush/v002/?preview=1`
- `scrollWidth = clientWidth = 1264`
- `scrollHeight = clientHeight = 625`
- one visible main region
- Canvas `toDataURL()` length: `250918`
- controls: `0`
- surrounding studio furniture hidden

The visual review showed the field filling the isolated frame. Broad dark strokes, dashed refused routes, and pale/orange return wounds remain legible as one material tableau. The full page gives the canvas the dominant surface below the opening title.

## Public integration evidence

These local routes and assets returned HTTP `200`:

- `/chantiers/p5-brush/v002/`
- `/chantiers/p5-brush/v002/?preview=1`
- `/chantiers/p5-brush/v002/style.css`
- `/chantiers/p5-brush/v002/sketch.js`
- `/chantiers/p5-brush/v002/engine.mjs`
- `/chantiers/p5-brush/v002/README.md`
- `/chantiers/p5-brush/v002/metrics.json`
- `/chantiers/p5-brush/v002/critiques.json`
- `/courants/brush/`
- `/courants/`
- `/oeuvres/`
- `/atelier/`
- `/journal/`

The currents index shows Brush v002 as its latest candidate. The works index shows seven version records, with Brush v001 still labelled `held interaction`. The atelier wall shows the real v002 preview with `creation`, `critique`, and `progression` annotations. The journal records the transition and leaves the material/perceptual gate open. WebGPU remains the only current without a tableau.

## Independent perceptual review — HOLD

An independent critic reviewed the first frame and the autonomous progression on 2026-08-31 without editing the work. The field reads as a restrained autonomous tableau and the wound is visible, but the relationship between wound and later stroke detour is not perceptually singular enough to carry the work without the explanatory copy.

**Required revision:** re-compose the wound and the first affected later stroke so the material consequence is visible on the canvas before requesting a fresh critique.

## Remaining gates

1. Fresh independent perceptual critique after the required revision.
2. Truth of production deployment: this work is local and has not been committed or deployed.

An earlier browser console collector emitted anonymous exception events without messages on several otherwise functioning local routes. A cache-disabled CDP exception probe later traced these to first-rAF negative stage selection in three sibling timelines; all four autonomous timeline sketches now clamp their stage index, and the post-fix currents probe reports zero exceptions. The exact local responsive/reduced-motion and direct-route touch audits are recorded in `2026-08-31-responsive-reduced-motion-matrix.md`.

## Latest independent perceptual review — HOLD

The latest independent browser review found the isolated preview convincing and the v001/v002 lineage honest. It also found that the normal route is text-led rather than tableau-led, and that the later overall curvature does not show an unmistakable local detour around an inherited wound; the pale marks can still read as overlays. The v002 accessible canvas and the inspected responsive surface showed no visible defect, but console inspection returned blank exception entries after navigation, which remains an unresolved warning.

The earlier local `KEEP` is retained as history but does not clear this later gate. Brush v002 remains `candidate / held after latest independent perceptual critique` and is not promoted. The next falsifiable move is to make one or two later strokes visibly detour around retained wounds, re-compose the normal route so the canvas leads, then repeat both exact-route visual checks and the console check.
