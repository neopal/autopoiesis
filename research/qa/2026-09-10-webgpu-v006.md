# WebGPU v006 — local browser gate — 2026-09-10

## Scope

- Work: `webgpu-2026-09-10`
- Study: `/studies/webgpu/v006/`
- Canonical route: `/works/webgpu-2026-09-10/`
- Status: `candidate / held for independent perceptual review`
- Local server: `python -m http.server 4173`
- Browser: Playwright with Google Chrome headless; no headed browser, CDP, remote debugging, or real profile

## Structural evidence

The deterministic engine test passed with 480 agents, 14 stages, a five-record memory cap, 191 final braided agents, 183 final rejoined agents, route separation `25.924648974289024`, and a latest-detour field delta of `1.548830848761373` against the latest-unlinked reconstruction. Visitor coordinates are clamped to `{x: 0.92, y: 0.08}` in the boundary test. Rebuilding after `Delete` reproduced the exact prior agent and archive state.

## Browser evidence

`probe.mjs` produced `results.json` with no failures.

- 10/10 canonical runs passed at `320x568`, `390x844`, `768x1024`, `1280x800`, and `1920x1080`, in both normal and reduced-motion modes.
- At all ten runs, outer `innerWidth`, `clientWidth`, and `scrollWidth` matched. Embedded study frames also had no horizontal overflow and matched their own client geometry.
- Normal-motion canonical runs opened at stage `01 / 14` with `0 detours`; reduced-motion runs settled at stage `14 / 14` with `5 detours`.
- Raw interactive preview at `390x844` stayed in preview mode, had `innerWidth=clientWidth=scrollWidth=390`, and all three controls measured `44px` high.
- Pointer click changed `0 detours` to `1 detour` and paused the tableau. `Delete` returned to `0 detours` and restored the exact prior canvas data URL.
- `Enter` on the focused canvas changed `0 detours` to `1 detour`; `Delete` returned to `0 detours`.
- `release census` returned to `stage 01 / 14` and `0 detours`.
- Static blind preview at `390x844` rendered a canvas, hid controls and readout, and had no horizontal overflow.
- Direct `/studies/webgpu/v006/` redirected to `/works/webgpu-2026-09-10/`.
- Canonical, raw interactive, and static runs recorded empty console, page-error, request-failure, and HTTP 400+ arrays.

Evidence files: `research/qa/proofs/webgpu-v006/results.json`, `probe.mjs`, and twelve PNG captures.

## Critique boundary

The unresolved art-gate item is an independent caption-free comparison with detour cuts, route threads, labels, and readout hidden. The record remains `candidate / held`; no perceptual promotion is claimed. GitHub SHA, production deployment, and deployed Journal readback are also pending.
