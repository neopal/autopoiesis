# WebGPU v009 QA — 2026-09-13

## Decision

**Candidate / held.** The v009 tableau is real, deterministic, structurally reversible, and locally browser-verified. It is not marked exhibition-ready: independent caption-free perceptual review and provider revision linkage remain unresolved.

## Artistic contract

- **Hypothesis:** a crowd becomes an archive when an absence can move against its own passage.
- **Changed rule:** v008 delayed a relay into a second downstream passage; v009 reverses a bounded packet against the main flow, turns it at a seam, then rejoins downstream.
- **Visible consequence:** agent coordinates carry a negative x displacement during the reversal window and a later rejoin offset; the witness route is not the only carrier of the rule.
- **Interaction:** pointer or Enter/Space creates the same bounded countercurrent; Delete/Backspace or lift latest rebuilds the exact prior field; release current resets the seeded sequence.
- **Falsifier/deletion:** delete v009 if the reverse packet and later rejoin are not legible with witnesses, labels, and readouts hidden, or if the action changes only decoration.

## Deterministic engine evidence

`studies/webgpu/v009/engine.mjs` reports 17 stages, 480 agents, memory limit 6, final memory 6, 253 counterflow agents, 198 reverse agents, 213 rejoined agents, countercurrent strength `0.329990931876258`, final return load `50.78703082941448`, latest return load `2.362554863259994`, and latest lifted field delta `4.860632377932714`. The visitor point `{x: 1.4, y: -0.2}` is bounded to `{x: 0.92, y: 0.08}` and produces a reverse route before rejoining.

## Local headless browser evidence

Source: `research/qa/proofs/webgpu-v009/results.json` and `probe.mjs`, using Python HTTP server on port 4173 and Playwright + Google Chrome headless.

- Canonical route: `/works/webgpu-2026-09-13/` rendered the v009 iframe tableau before generated title, Journal, critique, evidence, and neighbouring-work prose.
- Five viewports × normal/reduced motion: **10/10 passed** at 320×568, 390×844, 768×1024, 1280×800, and 1920×1080.
- Overflow: outer and embedded `innerWidth`, `clientWidth`, and `scrollWidth` matched in all matrix runs; the 320px long-heading min-content defect was diagnosed to the WebGPU canonical route and fixed with a scoped min-width/break-anywhere rule.
- Runtime: zero console messages, page errors, failed requests, and HTTP 400+ responses across canonical, raw, static-blind, and Journal runs.
- Raw interaction at 390×844: pointer 0→1 countercurrent, changed the canvas PNG signature, Delete 1→0 with exact canvas signature restoration, Enter 0→1, Delete 1→0, release 0 and stage 01/17.
- Controls: three raw controls measured 44 CSS px each.
- Static blind preview: canvas remained visible while editorial furniture, controls, readout, labels, and route notation were hidden; no overflow.
- Direct raw path redirected to `/works/webgpu-2026-09-13/`.
- Journal readback rendered exactly one `#journal-webgpu-2026-09-13` entry with the recorded title and canonical Journal href.
- 13 PNG captures were written: 10 matrix captures, raw interaction, static blind, and Journal.

## Unresolved

1. Independent caption-free perceptual comparison with route witnesses, labels, and readout hidden.
2. Provider revision linking a future stable-alias deployment to the eventual GitHub SHA.
