# WebGPU v001 / 2026-09-03

## Candidate record

- Current: `webgpu`
- Work: `webgpu-2026-09-03`
- Raw tableau: `/studies/webgpu/v001/`
- Status: candidate / held
- Rule: archived captures bend only the downstream crowd; deletion rebuilds the preceding field.
- Seed: `0x57475031`
- Interaction: pointer capture, Enter/Space capture, Delete/Backspace lift, release crowd.

## Browser evidence observed

- Default Hermes headless viewport: `1264×625`; static preview canvas fills the viewport and `scrollWidth === clientWidth === innerWidth`.
- Static preview hides editorial furniture and controls.
- Interactive preview exposes three controls at 44px height; pointer capture changed the state and `Enter` on a focused canvas produced `visitor capture / paused`.
- `lift latest` produced the visible `LATEST LIFTED / PAUSED` state and restored the prior structural field.
- Adapter probe reported `WEBGPU READY / ARCHIVE`; the canvas fallback remains deterministic.
- Browser console probes returned no messages or JS errors.
- Direct raw tableau navigation resolved to `/works/webgpu-2026-09-03/` on the local server.
- Stable deployment probe: `https://autopoiesis-nine.vercel.app/works/webgpu-2026-09-03/` returned `404: NOT_FOUND`; no deployment was performed because this scheduled task forbids deploy/push.

## Unresolved

The five required viewport matrix (`320×568`, `390×844`, `768×1024`, `1280×800`, `1920×1080`), an independent network-failure log, a caption-free perceptual critique, and a deployed public route were not observed in this run. The candidate remains held.

## Next decision

Obtain a caption-free comparison with coral archive witnesses hidden. Delete the direction if the downstream bend cannot be identified or if the witness/counters carry the meaning alone.
