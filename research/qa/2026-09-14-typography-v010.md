# Handwriting v010 browser release evidence — 2026-09-14

## Scope

Local headless release readback for the scheduled typography slot. Base URL:
`http://127.0.0.1:4173`. The work remains **candidate / held**: runtime and
responsive evidence passed, but no independent caption-free perceptual reviewer
was available in this unattended run and production/provider revision evidence
is still separate.

## Observed artifact

- Canonical: `/works/typography-2026-09-14/`
- Raw preview: `/studies/handwriting/v010/?preview=1&interaction=1`
- Blind still: `/studies/handwriting/v010/?preview=1&static=1&blind=1`
- Tableau: v010 deterministic Canvas 2D hinge field

## Browser matrix

Playwright + Chromium headless exercised the canonical route at
`320×568`, `390×844`, `768×1024`, `1280×800`, and `1920×1080`, each in normal
and reduced-motion modes: **10/10 passed**.

- Normal runs opened at stage `0`, memory length `0`, and `paused: false`.
- Reduced-motion runs settled at stage `13`, memory length `6`, and
  `paused: true`.
- At every run, `innerWidth == clientWidth == scrollWidth` at the outer page
  and embedded tableau frame.
- The embedded tableau was before the generated work heading in all ten runs.
- Canvas bounds were positive in all ten runs.

## Interaction readback

At `390×844` raw preview:

1. Pointer gesture: memory `0 → 1`, paused, canvas signature changed.
2. Enter on focused canvas: memory `1 → 2`.
3. `lift latest`: memory `2 → 1`, exact pointer-state PNG signature restored.
4. `release sequence`: memory `1 → 0`, stage `0`, unpaused.
5. Initial controls were all `44px` high; `make hinge` and `release sequence`
   were active, while `lift latest` was correctly disabled with empty memory.
6. Outer geometry remained `390px` wide throughout.

The static blind readback rendered the final stage with canvas visible, all
editorial furniture hidden, stage `13`, memory length `6`, and no document
overflow. The Journal readback rendered exactly one
`#journal-typography-2026-09-14` entry with the recorded title and
`/works/typography-2026-09-14/#journal` link.

## Browser/network issues

The probe collected zero console messages, page errors, failed requests, and
HTTP 400+ responses across the ten canonical runs, raw interaction, blind
preview, and Journal page.

## Evidence files

`results.json`, `probe.mjs`, and 13 PNG captures in this directory:

- ten canonical viewport/motion captures;
- `raw-interaction-390x844.png`;
- `static-blind-390x844.png`;
- `journal-390x844.png`.

## Release boundary

The authorized Vercel CLI command
`npx vercel --prod --yes --scope lairpa-hotmailfrs-projects` returned
`scope-not-accessible` and produced no deployment URL. The stable alias was
then read independently after the GitHub push and did serve the v010 content:
HTTP 200 JSON with exactly one target record, HTTP 200 tableau-first canonical
work at `390×844`, HTTP 200 Journal with one target anchor, HTTP 200 raw preview
with the same reversible pointer/keyboard/lift/release sequence, and HTTP 200
SVG favicon. The production probe collected zero browser/network issues.

Production screenshots and readback are in `production-results.json`,
`production-probe.mjs`, `production-canonical-390x844.png`,
`production-journal-390x844.png`, and `production-raw-interaction-390x844.png`.
The provider did not report a revision linking the stable alias to GitHub SHA
`864f8fbed7d8a0a6e07c9f7a515a79998ba5ddbb`, so that provenance remains
unresolved. No independent caption-free perceptual comparison was claimed. The
unresolved artistic question remains whether the angular pivot and carried exit
stay legible with all witnesses and explanatory furniture hidden.
