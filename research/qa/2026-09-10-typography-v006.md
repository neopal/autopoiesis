# QA — typography v006 / 2026-09-10

**Status:** candidate / held
**Local server:** `http://127.0.0.1:4173`
**Production alias:** `https://autopoiesis-nine.vercel.app`
**Canonical work:** `/works/typography-2026-09-10/`
**Raw preview:** `/studies/handwriting/v006/?preview=1&interaction=1`

## Concept and gate

The changed rule is a contagious counterform: a remembered refusal opens one aperture, nearby routes share its mouth and missing interval, then re-emerge with a common phase and exit shift. The counterform is computed into route points and missing spans; it is not a marker drawn over an unchanged field.

The falsifier is caption-free: hide the gap witnesses, labels, and readout. If the common disappearance and changed after-route cannot be located in the handwriting itself, v006 is deleted. That independent perceptual comparison remains unresolved.

## Browser evidence

Headless Playwright ran the canonical work page locally at `320x568`, `390x844`, `768x1024`, `1280x800`, and `1920x1080`, in both `no-preference` and `reduce` motion contexts: **10/10 passed**.

- All runs returned HTTP 200.
- Top-level `innerWidth === clientWidth === scrollWidth`; embedded preview frames had no horizontal overflow.
- The canonical stage rendered before the explanatory heading.
- The embedded tableau rendered a non-empty Canvas 2D surface; reduced-motion settled at stage 10 with six remembered gaps.
- All three action buttons measured 44 CSS pixels in the raw interactive preview.
- Console message, page-error, failed-request, and HTTP 400+ arrays were empty.

## Interaction evidence

On the raw interactive preview at `390x844`:

- Initial state: stage 0, unpaused, memory 0, structural gap routes 0.
- Pointer click: memory 0 → 1, paused, 12 shared structural routes.
- Focused canvas + `Enter`: memory 1 → 2, changed route field retained.
- `lift latest`: memory 2 → 1 and exact route signature restored to the post-pointer state.
- `release sequence`: stage 0, memory 0, unpaused.

A reduced-motion `static=1&blind=1` preview also returned HTTP 200, hid editorial furniture, and remained 390px overflow-free.

The production alias was then read back with Playwright at `390x844`: **passed**. `/studio/data/works.json` returned the `typography-2026-09-10` record; `/journal/` rendered `#journal-typography-2026-09-10` once with the registered title; the canonical work rendered its heading and one embedded v006 Canvas; and the raw preview began at stage 0, changed memory 0 → 1 with a pointer click, and produced 12 shared structural routes. All production console, page-error, failed-request, and HTTP 400+ arrays were empty. A separate HTTP check returned 200 for `/favicon.ico` after propagation.

## Captures

`results.json`, `production-results.json`, the two Playwright probes, and 12 PNG captures are stored beside this report in `research/qa/proofs/typography-v006-2026-09-10/`.

## Unresolved

- GitHub and production content readback are complete: local and `origin/main` resolve to `f4023506b46f8bfe31f6ec284f3ed8463d8a2381`; the stable alias serves the registered record and Journal anchor/title.
- No independent caption-free perceptual reviewer has confirmed that the common opening and after-route carry the concept without witnesses or prose. Keep the record **candidate / held**.
