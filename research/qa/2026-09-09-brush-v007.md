# Brush v007 — local release evidence — 2026-09-09

## Candidate / held

The local browser gate passed. The work remains held because the independent caption-free perceptual comparison has not been observed.

## Route and matrix

- Canonical local route: `http://127.0.0.1:4173/works/brush-2026-09-09/`
- Raw preview: `http://127.0.0.1:4173/studies/p5-brush/v007/?preview=1&interaction=1`
- Viewports: `320x568`, `390x844`, `768x1024`, `1280x800`, `1920x1080`
- Modes: normal and `prefers-reduced-motion: reduce`
- Result: `10/10` canonical runs passed.

Top-level `innerWidth`, `clientWidth`, and document `scrollWidth` matched on all matrix runs. Each embedded study document had equal `clientWidth` and `scrollWidth`. The current’s mobile timeline is a bounded horizontal navigation rail (`nav.work-timeline-bar`) rather than page overflow.

## Interaction evidence

At the 390px normal run:

- `MAKE A SEAM`: memory `0 → 1`, state `seam made / paused`.
- Focused canvas + `Enter`: memory `1 → 2`.
- `LIFT LATEST`: memory `2 → 1`, state `latest seam lifted`; the artwork-region pixel signature matched the prior one-seam state exactly.
- `RELEASE SEQUENCE`: memory returned to `0`.
- All three controls measured `44 CSS px` high.

## Console and network

- Empty console message buffer and zero page errors on all ten canonical runs.
- Zero failed requests and zero HTTP 400+ responses on all ten canonical runs.
- Raw preview at 390×844: preview and interactive classes present, editorial furniture hidden, canvas and controls present, no page overflow, no page errors, no failed requests, no HTTP 400+ responses.

## Evidence files

`research/qa/proofs/brush-v007-2026-09-09/results.json` plus ten canonical PNG captures and one raw-preview PNG capture.

## Unresolved

Independent caption-free perceptual comparison with seam witnesses, notation, labels, and readout hidden; production deployment readback.
