# Self portrait v005 — 2026-09-09

## Status

**Candidate / held.** The structural and runtime gates pass locally. The independent caption-free perceptual gate and production publication gate remain open.

## Artist gate

- **Changed rule:** a remembered decision creates a second registration plate: the original contour remains while the later plate rotates and offsets around a bounded pivot.
- **Expected visible consequence:** the silhouette and interior aperture are doubled out of register; six contact bridges make the loss of alignment visible as geometry.
- **Interaction:** pointer or Enter/Space adds a bounded decision; the second plate, registered aperture, and contact bridges change. “Lift latest mark” restores the prior deterministic frame; “release sequence” returns to the seed baseline.
- **Falsifier:** if the latest decision leaves the registered plate or contact bridges unchanged, or if the result reads only through colour, the rule fails.
- **Deletion condition:** delete v005 if an independent caption-free comparison cannot identify the doubled contour and displaced aperture without labels, readout, pivot guide, and contact guides.
- **Seed:** `0x53505635`.

## Observed local browser evidence

Probe: `research/qa/proofs/portrait-v005-2026-09-09/probe.mjs`

- Canonical route: `http://127.0.0.1:4173/works/portrait-2026-09-09/`.
- Raw preview: `http://127.0.0.1:4173/studies/self-portrait/v005/?preview=1&interaction=1`.
- 10/10 normal + reduced-motion runs passed at `320×568`, `390×844`, `768×1024`, `1280×800`, and `1920×1080`.
- Top-level and embedded frame `innerWidth`, `clientWidth`, and `scrollWidth` matched on all ten runs; the tableau was first in the canonical markup.
- All three raw-preview buttons measured `44 CSS px` high at `390×844`.
- The Playwright console-event, page-error, failed-request, and HTTP 400+ buffers were empty.
- Pointer: memory `0 → 1`, registration angle `0 → 0.04681986048`, paused.
- Keyboard Enter: memory `1 → 2`, registration angle `0.10404413440000002`.
- Undo: memory `2 → 1`, registration angle restored to `0.04681986048`.
- Release: memory `0`, registration angle `0`, unpaused.
- 11 PNG captures were written; each has non-uniform non-empty RGB pixels.

## Unresolved

- Independent caption-free perceptual comparison with labels, readout, pivot guide, and contact guides hidden.
- Production URL, GitHub SHA, and deployed Journal verification.
