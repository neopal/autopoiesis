# QA — Pure SVG v001 / primitive budget

Date: 2026-08-31
Scope: local working tree in `C:\Users\ASUS\autopoiesis`

## Artistic decision

Pure SVG is promoted from a held question to an **active current with one autonomous candidate**, not to a finished or validated period.

The work tests one rule: a silhouette spends a fixed primitive budget, rejects one structural point, and carries that cut into later geometry. The visitor does not operate the timeline.

## Implementation observed

- Route: `/chantiers/pure-svg/v001/`
- Renderer: native SVG, no p5 dependency
- Seed: `0x4d555449`
- Timeline: 8 deterministic stages
- Primitive budget: 18
- Input: none; no button, input, select, pointer, click, key, or visitor control
- Preview: `?preview=1` hides the page furniture and keeps the SVG field visible
- Visual layers: dashed refused draft, pale accepted outline, orange cut, inherited memory marks

## Engine evidence

Executed against `chantiers/pure-svg/v001/engine.mjs`:

```text
stages: 8
primitiveBudget: 18
memoryAtStage5: 2
max displacement with memory: 0.039090172150622604
replay: true
```

Adversarial local check at stage 5:

```text
full memory: 2
memory after deleting one inherited cut: 1
changed later points: 5
```

This proves a deterministic structural dependency in the engine. It does not yet prove that the dependency reads strongly enough as art.

## Public integration

- `galerie/data/studio.json` records Pure SVG as `active`, `v001`, one work.
- `galerie/data/evolutions.json` records the candidate and three pending critiques.
- `/courants/` shows the latest Pure SVG tableau.
- `/courants/pure-svg/` shows its work and the `creation → critique → progression` annotations.
- `/oeuvres/` includes Pure SVG among recorded versions.
- `/atelier/` replaces the dormant placeholder with the actual preview while retaining the held-for-critique label.
- `/journal/` records the promotion and links this report.

## Browser observation

Observed locally at `http://127.0.0.1:4176/chantiers/pure-svg/v001/` and with `?preview=1`:

- the SVG field paints on first load;
- the preview route fills the frame and hides page furniture;
- the stage readout advances autonomously;
- a fresh `window.onerror` / unhandled-rejection handler recorded no new errors during a 4.6-second run;
- the visual consequence is present but still reads partly as a diagram, so perceptual promotion remains held.

## Open gate

The candidate remains held until browser observation and independent perceptual critique answer one question: does the inherited cut visibly alter the animal's decision, or does the result still read as a decorative outline with a diagrammatic scar?

The three remaining currents — Self portrait, Naive art, and WebGPU — stay dormant and must not receive fabricated tableaux. Brush remains a visitor-operated interaction, not autonomous production.

## Release boundary

This report is local evidence only. No deployment, commit, or period claim is made here. A new responsive/reduced-motion browser matrix is still required before any public release claim.
