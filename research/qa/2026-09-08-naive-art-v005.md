# Naive art v005 daily studio record — 2026-09-08

## Scope and candidate

This scheduled rotation targeted Naive art (`naive`) on `2026-09-08`; the pre-run slot was empty. The canonical sources were `studio/data/studio.json` (`mutine-studio/v2`) and `studio/data/works.json` (`mutine-works/v1`). Field tests and stimuli were not treated as work records.

A new candidate tableau was implemented at `/studies/naive-art/v005/` and recorded exactly once as `naive-2026-09-08`. The record is `candidate / held`; no promotion to complete is claimed.

**Title:** The path keeps the wrong turn.

- **Changed rule:** v004 made a shared false vanishing point. v005 abandons that destination rule: each retained correction becomes a signed hinge vector replayed from three different origins — house edge, ground, and route.
- **Visible consequence:** the refused panel is flat and direct; the kept panel repeats the same left/right elbow sequence across its house hinge, ground, and path.
- **Deterministic state:** seed `0x4e413035`, ten stages, 32-shape budget, nine retained turns at the settled state, deterministic SVG path geometry.
- **Interaction:** pointer/tap or the `KEEP A WRONG TURN` button adds a bounded visitor turn; Enter/Space follows the keyboard path; `UNDO LAST` rebuilds the exact preceding field; `RELEASE SEQUENCE` returns to the seeded sequence.
- **Falsifier:** hide labels and witness dots. If the path and house no longer carry the same turn family, the rule is ornamental.
- **Deletion condition:** delete v005 if two caption-free comparisons cannot identify the repeated elbow pattern, or if it reads as a generic playful zigzag.

## Changed files

- `studies/naive-art/v005/README.md`
- `studies/naive-art/v005/critiques.json`
- `studies/naive-art/v005/engine.mjs`
- `studies/naive-art/v005/index.html`
- `studies/naive-art/v005/metrics.json`
- `studies/naive-art/v005/sketch.js`
- `studies/naive-art/v005/style.css`
- `tests/naive-art-v005.test.mjs`
- `studio/data/works.json`
- `works/naive-2026-09-08/index.html` (canonical generated shell)
- `tests/catalog-architecture.test.mjs`, `tests/daily-catalog.test.mjs`, `tests/evolution-catalog.test.mjs` (updated register-count/order expectations from 27 to 28 for this real daily record)
- `research/qa/proofs/naive-art-v005/probe.cjs`
- `research/qa/proofs/naive-art-v005/results.json`
- `research/qa/proofs/naive-art-v005/canonical-*.png` (ten normal/reduced viewport captures)
- `research/qa/2026-09-08-naive-art-v005.md`

## Repository and structural evidence

- `studio/data/studio.json`: PASS — valid JSON; six registered currents.
- `studio/data/works.json`: PASS — valid JSON; 28 records; target appears exactly once.
- Current/date uniqueness: PASS — no duplicate `currentId/date` pairs.
- Target raw tableau: PASS — v005 index, engine, sketch, style, metrics, critiques, and README exist.
- Target canonical page: PASS — `works/naive-2026-09-08/index.html` exists and carries `data-catalog-work-detail="naive-2026-09-08"`.
- Target journal relation: PASS — `journal-naive-2026-09-08`; canonical shell points to `/journal/#journal-naive-2026-09-08`.
- Critique condition: PASS — four named critiques are recorded; status remains held.
- Lineage: PASS — `naive-2026-09-07` → v005.

## TDD and automated verification

- **RED:** `node --test tests/naive-art-v005.test.mjs` initially failed with `ERR_MODULE_NOT_FOUND` because v005 did not yet exist.
- **GREEN:** `node --test tests/naive-art-v005.test.mjs`: **PASS — 6/6**.
- **Full suite:** `npm test`: **PASS — 139/139 tests, 0 failed, 0 skipped, 0 todo**.
- **JavaScript syntax:** **PASS** — `node --check studies/naive-art/v005/engine.mjs` and `node --check studies/naive-art/v005/sketch.js`.
- **Whitespace:** **PASS** — `git diff --check`.
- **Added-line security scan:** no matches for hardcoded-secret, shell-injection, eval/exec, unsafe-deserialization, or SQL-construction patterns in the changed source.
- **Independent reviewer:** not available in this scheduled runtime; no independent approval is claimed.

## Local browser evidence

The local server was `python -m http.server 4173 --bind 127.0.0.1`. The canonical route was exercised headlessly with the installed Chrome executable; no GUI, real profile, CDP, remote debugging, or CUA was used.

Canonical route:

`http://127.0.0.1:4173/works/naive-2026-09-08/`

The saved evidence is `research/qa/proofs/naive-art-v005/results.json` and ten PNG captures.

- Canonical shell and embedded v005 tableau: **10/10 runs rendered**.
- Viewport matrix: **10/10 passed** — `320x568`, `390x844`, `768x1024`, `1280x800`, and `1920x1080`, each in normal and reduced-motion modes.
- Probe validity: at every run, `innerWidth === clientWidth === scrollWidth`; no horizontal overflow was observed.
- Tableau ordering: canonical DOM first child is `studio-path`; the full-artwork iframe is loaded before the ledger/timeline content.
- Preview mode: embedded raw tableau reports `preview-mode` and `interactive-preview`.
- Reduced motion: each reduced run settled at `stage 10 / 10` with `9 turns retained`; normal motion opens at `stage 04 / 10` with `3 turns retained`, making the changed rule visible immediately.
- Touch targets: all three buttons measured at least 44 CSS px high; `320x568` measured 50 CSS px.
- Initial console/network: **0 console messages, 0 page errors, 0 failed requests, 0 first-party responses >=400** across the viewport matrix.
- Interaction: `KEEP A WRONG TURN` changed the route and state from `3` to `4` turns and paused the sequence; Enter changed it to `5` turns with a further route change; `UNDO LAST` returned to `4` turns and exactly restored the post-button route signature; `RELEASE SEQUENCE` returned to stage 01 / 10 and exactly restored the initial route signature.
- Raw route behavior: direct `/studies/naive-art/v005/` navigation redirected to `/works/naive-2026-09-08/`; direct `?preview=1&interaction=1` navigation stayed on the raw route, loaded the SVG field, reported both preview classes, and exposed the controls.

## Artistic doubt and release boundary

The structural and browser gates are green locally. The candidate remains **held** because no independent caption-free perceptual comparison has yet been observed with labels and witness dots hidden. The key doubt is whether the repeated house/ground/path elbow reads as one remembered hand movement rather than a pleasing generic zigzag. Public production visibility, GitHub synchronization, and deployed Journal verification remain pending the authorized release step.

**Archive decision:** record the candidate; do not call it exhibition-ready.
