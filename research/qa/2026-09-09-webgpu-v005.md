# WebGPU v005 — local browser gate — 2026-09-09

## Scope

- Work: `webgpu-2026-09-09`
- Study: `/studies/webgpu/v005/`
- Canonical route under test: `/works/webgpu-2026-09-09/`
- Status: `candidate / held for independent perceptual review`
- Local server: `python -m http.server 4173`
- Browser: Playwright with Google Chrome headless; no headed browser, CDP, remote debugging, or real profile

## Structural evidence

The deterministic engine test passed with 480 agents, 13 stages, a five-record memory cap, a final index shear of `226.05846153140482`, and a latest-index deletion field delta of `1.5959626167100431`. The visitor point was bounded to `{x: 0.92, y: 0.08}`. Rebuilding after `Delete` reproduced the exact prior engine state.

## Browser evidence

`probe.mjs` produced `results.json` with no failures.

- 10/10 canonical runs passed at `320x568`, `390x844`, `768x1024`, `1280x800`, and `1920x1080`, in both normal and reduced-motion modes.
- At all ten runs, top-level `innerWidth`, `clientWidth`, and `scrollWidth` matched. Embedded study frames had no horizontal overflow.
- Raw interactive preview at `390x844` stayed in preview mode, had `innerWidth=clientWidth=scrollWidth=390`, and all three controls measured `44px` high.
- Pointer click changed `0 indexes` to `1 index` and paused the tableau. `Delete` returned to `0 indexes` and restored the exact blind canvas signature.
- `Enter` on the focused canvas changed `0 indexes` to `1 index`; `Delete` returned to `0 indexes`.
- `release census` returned to `stage 01 / 13` and `0 indexes`.
- Static blind preview at `390x844` rendered a canvas, hid controls, and had no horizontal overflow.
- Direct `/studies/webgpu/v005/` redirected to `/works/webgpu-2026-09-09/`.
- Canonical, raw interactive, and static runs recorded empty console, page-error, request-failure, and HTTP 400+ arrays.

Evidence files: `research/qa/proofs/webgpu-v005/results.json`, `probe.mjs`, and twelve PNG captures.

## Critique boundary

The unresolved art-gate item is an independent caption-free comparison with archive cuts, route threads, labels, and readout hidden. The record remains `candidate / held`; no perceptual promotion is claimed.
