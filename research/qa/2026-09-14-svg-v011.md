# Pure SVG / v011 QA — 2026-09-14

## Candidate record

- **Current/date:** svg / 2026-09-14
- **Work:** `svg-2026-09-14`
- **Study:** `/studies/pure-svg/v011/`
- **Canonical page:** `/works/svg-2026-09-14/`
- **Title:** The animal keeps a hinge.
- **Status:** candidate / held
- **Changed rule:** a remembered refusal rotates the downstream contour around a crease, opens an interior gate, and makes three routes fold or brace around the turn.
- **Deterministic state:** `SEED = 0x53564741`; 15 stages; primitive budget 22; memory window 4; three routes.
- **Hypothesis:** a refusal can become a hinge in the animal’s body rather than a decorative mark or an added load.
- **Visible consequence:** the silhouette turns at a real crease, the gate shifts, and downstream route geometry changes posture; witness threads, pins, labels, and readout are overlays, not the causal state.
- **Interaction:** click/tap the field, press Enter/Space after focusing the SVG, or choose `place a hinge`; `lift latest` restores the preceding field; `release the body` clears memory and resumes the sequence.
- **Falsifier:** hide hinge witnesses, gate notation, labels, and readout. If the silhouette does not retain a legible fold and the routes do not visibly answer it, the direction fails.
- **Deletion condition:** delete v011 if lifting the latest hinge leaves downstream contour or route coordinates unchanged, or if the hinge only exists as witness notation.

## Lineage

`pure-svg-v010` → v011: from a distributed counterweight to a hinged contour whose fold is carried downstream by the body and routes.

## Engine evidence

- `node --test tests/pure-svg-v011.test.mjs`: **4 passed, 0 failed**.
- `npm run test`: **311 passed, 0 failed, 0 skipped, 0 todo**.
- `node --check studies/pure-svg/v011/sketch.js`: **passed**.
- `node --check studies/pure-svg/v011/engine.mjs`: **passed**.
- `git diff --check`: **passed**.
- Settled engine state: stage `15/15`, memory `4`, hinged routes `3`, folded routes `2`, braced routes `1`.
- Latest-unlinked comparison: contour delta `1.0169149299281575`; route delta `1.8281771035796759`.
- Visitor input clamps to `x: 0.06..0.94`, `y: 0.16..0.84`; `applyHinge()` is deterministic.
- Exact undo returns the prior contour/route/memory signature.

## Browser evidence (local headless)

Command: `node research/qa/proofs/svg-v011-2026-09-14/probe.mjs`

- **11/11 runs passed:** canonical 5 viewports × normal/reduced motion, raw interactive 390×844, and static blind 390×844.
- Viewports: `320×568`, `390×844`, `768×1024`, `1280×800`, `1920×1080`.
- Every canonical run recorded `innerWidth == clientWidth == scrollWidth`; raw and blind runs also recorded no horizontal overflow.
- Every canonical run rendered the artwork iframe before generated title, Journal, critique, evidence, and neighbouring-work prose.
- Embedded SVG remained visible at every viewport; all three interaction controls measured 44 CSS px at 390×844 and canonical embedded runs.
- Normal-motion canonical runs opened at stage `01/15`, memory `0`; reduced-motion canonical and blind runs settled at stage `15/15`, memory `4`.
- Pointer changed memory `0 → 1` and paused the tableau; Enter changed memory `1 → 2` and changed the route field; `lift latest` changed memory `2 → 1` and restored the exact pointer-state SVG signature; `release the body` returned memory to `0` with `paused=false`.
- Static blind mode kept the SVG visible while hiding readout, controls, labels, hinge witnesses, threads, gate, and pin.
- Zero console messages, page errors, failed requests, and HTTP 400+ responses were observed across the local runs.
- 13 PNG captures were written: ten canonical viewport/motion captures, two raw interaction captures, and one static blind capture.
- Proof files: `research/qa/proofs/svg-v011-2026-09-14/results.json`, `probe.mjs`, and the 13 PNG captures.

## Release boundary

The artifact, register record, canonical route, Journal data path, structural engine, responsive matrix, and interaction readback are real and verified locally. The candidate remains **held**. No exhibition-ready claim is made because independent caption-free perceptual comparison with witnesses hidden has not been performed, and provider revision linking the stable production alias to the eventual GitHub SHA is not yet available.
