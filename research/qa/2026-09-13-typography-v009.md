# Typography v009 — local release gate

**Date:** 2026-09-13
**Current:** typography
**Work:** `typography-2026-09-13`
**Status:** candidate / held
**Local route observed:** `http://127.0.0.1:4173/works/typography-2026-09-13/`

## Changed rule

A remembered refusal becomes a lane switch. Two adjacent routes are forced through one event-owned seam, cross once, exchange vertical order, and continue downstream with a carried drift. The visitor can place a bounded switch, lift only the latest switch, or release the authored sequence.

## Local browser evidence

Headless Playwright 1.51.1, served from the repository at port 4173:

- **Viewport matrix:** 10/10 canonical runs passed at `320×568`, `390×844`, `768×1024`, `1280×800`, and `1920×1080`, each in normal and reduced-motion mode.
- **Geometry:** every canonical run recorded `innerWidth == clientWidth == scrollWidth`; the raw interactive and static blind 390×844 runs matched as well.
- **Tableau order:** all ten canonical runs rendered the v009 tableau iframe before the generated title, Journal, critique, evidence, and neighbouring-work prose.
- **Interaction:** raw pointer `0 → 1` switch and paused state; keyboard `Enter` `1 → 2`; lift latest `2 → 1` and restored the exact preceding route signature; release sequence returned to stage `00 / 13`, memory `0`, unpaused.
- **Reduced motion:** canonical reduced-motion runs settled at stage `12 / 13`, memory `5`, with `4` switched routes.
- **Accessibility:** all three raw controls measured `44px` high at 390px viewport width; canvas keyboard path was exercised.
- **Static blind state:** `?preview=1&interaction=1&blind=1` under reduced motion hid editorial furniture, controls, readout, switch witnesses, labels, and notation while retaining the canvas; no overflow.
- **Console/network:** zero console messages, page errors, failed requests, or HTTP 400+ responses across the canonical, raw interaction, and static blind runs.
- **Captures:** 12 PNG files written under `research/qa/proofs/typography-v009-2026-09-13/` — ten matrix captures, one interaction capture, and one static blind capture. The blind capture was non-flat: RGB channel spans `[236, 225, 204]` with 721 sampled colours.

Machine-readable evidence: `research/qa/proofs/typography-v009-2026-09-13/results.json` and `probe.mjs`.

## Automated gates

- `node --test tests/handwriting-v009.test.mjs` — passed during implementation.
- `node --test tests/daily-catalog.test.mjs tests/catalog-architecture.test.mjs tests/evolution-catalog.test.mjs` — passed, 16 tests.
- `npm test` — passed.
- `node --check studies/handwriting/v009/sketch.js` — passed.
- `node --check research/qa/proofs/typography-v009-2026-09-13/probe.mjs` — passed.
- `git diff --check` — passed on the final worktree.

## Critique and hold

The accepted structural critique is that the change must be carried by actual route geometry: one shared crossing seam, one order exchange, and a changed downstream exit; a coloured X or crossing index cannot substitute for paths. The interaction critique is satisfied locally by reversible, deterministic, five-event memory behavior.

The work remains **candidate / held**. Production canonical, Journal, and raw readback have not yet been observed. The independent caption-free perceptual review has not been completed: with witnesses, labels, and readout hidden, an independent viewer must still recognize the X-shaped crossing and exchanged exit order. Delete v009 if that comparison reads as a kink or generic wave rather than a structural lane exchange.

## Publication blocker

The requested independent code-review subagent is not exposed in this scheduled Hermes runtime; the tool catalog returned no matching reviewer/delegate capability. No reviewer verdict is claimed. The staged change is therefore intentionally **not committed, pushed, or deployed** until that fail-closed gate can run.
