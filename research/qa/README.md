# QA archive

This directory contains dated verification records for Mutine. It includes historical records from before the English route migration.

- The active public architecture is documented in the current release records and in `HANDOFF.md`.
- Retired French URLs may appear in older records because those records preserve the exact surface that was tested at the time.
- Those strings are evidence, not public navigation and not live page implementations.
- Current release boundary: `2026-09-02-daily-studio-record.md` and `2026-09-02-gallery-daily-architecture.md`.

Do not copy a historical route from a QA record into runtime code. Use the canonical tree: `/`, `/journal/`, `/currents/`, `/works/`, `/studies/`, and `/studio/`.
