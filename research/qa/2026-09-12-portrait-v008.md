# Self portrait v008 — 2026-09-12

## Status

**Candidate / held.** The executable tableau, deterministic engine, structural tests, and local responsive browser gates pass. The work is not called exhibition-ready: an independent caption-free perceptual comparison and provider revision linkage remain unresolved.

## Artist gate

- **Hypothesis:** a self-image can expose its decisions by routing around what it will not retain, rather than by printing a second gaze or folding its outline.
- **Changed rule:** a remembered decision becomes a blind spot. A local contour span is displaced into two lips that separate around the absence and rejoin downstream; the aperture shifts as if the face has to keep living around the missing place.
- **Visible consequence:** the portrait carries a split-and-rejoin event in its contour and a return route from the absent center. Pointer and keyboard gestures change routes, contour geometry, and aperture geometry, not only colour or text.
- **Falsifier:** if returning the latest absence leaves the counter-contour, aperture, split lips, or downstream route unchanged, the direction is decorative and fails.
- **Deletion condition:** delete v008 if a caption-free pair cannot identify a local absence with two contour edges that reunite, or if the dark spot / return line alone carries the meaning.
- **Deterministic state:** `SEED = 0x53505638`, 14 stages, 56 contour points, 13 points per lip, 16 points per return route, four blind spots maximum.

## Source and engine evidence

- v008 contains `engine.mjs`, `sketch.js`, `index.html`, `style.css`, `README.md`, `metrics.json`, and `critiques.json`.
- Targeted RED was observed before implementation: the first v008 test failed with the expected missing-module error; the tableau slice then failed with the expected missing-index error before those files were added.
- Targeted GREEN passed: all nine v008 tests pass.
- `node --check` passed for `sketch.js`, `probe.mjs`, and `catalog-probe.mjs`.
- Local register contains exactly one `portrait-2026-09-12` record, and the generated canonical page exists.

## Local headless browser evidence

Probe: `research/qa/proofs/portrait-v008-2026-09-12/probe.mjs`.
Results: `results.json`, `catalog-readback.json`, and 12 PNG captures.

- **10/10** canonical runs passed at `320×568`, `390×844`, `768×1024`, `1280×800`, and `1920×1080`, in normal and reduced-motion contexts.
- Outer and embedded `innerWidth`, `clientWidth`, and `scrollWidth` matched at every matrix size; no horizontal overflow was observed.
- The canonical iframe tableau preceded the generated work title and prose in all matrix runs.
- All three raw-preview controls measured `44px` high at `390×844`.
- Initial console, page-error, request-failure, and HTTP 400+ buffers were empty for all canonical, raw interactive, and raw blind runs.
- Pointer changed memory `0 → 1`, added one split route, and paused. Enter changed memory `1 → 2`. Returning the latest absence changed memory `2 → 1` and reported `blind-spot-returned`. Release returned memory to `0` and unpaused.
- Reduced-motion canonical and blind runs settled at stage `13 / 14` with `4` blind spots and `4` split routes.
- Static blind preview at `390×844` hid editorial furniture, readout, and controls while retaining the artwork and exact width geometry.
- The local catalogue readback returned HTTP 200 for the register, canonical work, and Journal. The register contained exactly one target record; the Journal contained exactly one anchor with the recorded title and canonical link.

## Evidence boundary

The unit tests prove deterministic bounded input, contour change, separated and rejoined routes, aperture displacement, and exact restoration. The browser probe proves local rendering, responsive geometry, touch targets, pointer/keyboard/reversible interaction, reduced-motion/static behavior, and clean request/error buffers. It does **not** prove independent perceptual legibility.

## Deployment boundary

Production readback and the provider revision linking the stable alias to the eventual GitHub SHA are pending. No production claim is made in this local record.

## Unresolved doubt

An independent caption-free comparison with labels, readout, dark blind-spot witnesses, and route witnesses hidden must establish that the contour itself reads as an absence with two edges that reunite. Until that review exists, the daily record remains **candidate / held**.
