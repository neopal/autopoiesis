# Pure SVG v008 — daily work QA record

**Date:** 2026-09-10
**Current:** Pure SVG (`svg`)
**Work:** `svg-2026-09-10`
**Status:** candidate / held

## Creative gate

- **Hypothesis:** a refusal can be passed onward through an animal instead of remaining as an echo, hinge, or weight.
- **Changed rule:** a remembered refusal stores a contour anchor and a later receiver. Downstream points alternate around that handoff, and later feet inherit the same lead/return order.
- **Visible consequence:** the body and gait change together; the relay knot and handoff thread are witnesses, not the claimed meaning.
- **Falsifier:** with relay witnesses, handoff threads, labels, and readout hidden, the contour must still switch sides around a receiver and the feet must still share the relay order.
- **Deletion condition:** delete v008 if unlinking the latest relay leaves downstream geometry unchanged, or if the body reads as a static animal with decorative signals.

## Implementation evidence

- Raw tableau: `studies/pure-svg/v008/index.html`
- Engine: `studies/pure-svg/v008/engine.mjs`
- Runtime: `studies/pure-svg/v008/sketch.js`
- Style: `studies/pure-svg/v008/style.css`
- Metrics and critiques: `studies/pure-svg/v008/metrics.json`, `critiques.json`
- Canonical work: `works/svg-2026-09-10/index.html`
- Register: `studio/data/works.json`
- Lineage: `pure-svg-v007` → `v008`
- Deterministic seed: `0x53564738`
- Structural run: stage 10, four remembered relays, three relayed routes; contour delta against latest-unlinked frame `0.7363219279077099`, route delta `0.49184277650604985`.

## Test-first record

1. Wrote `tests/pure-svg-v008.test.mjs` with the relay behavior before creating the v008 engine; the first run failed with the expected `ERR_MODULE_NOT_FOUND` for `v008/engine.mjs`.
2. Implemented the deterministic engine; the first structural test passed.
3. Added the browser contract test before the v008 HTML/runtime/style existed; it failed with the expected missing `index.html`.
4. Added the tableau and runtime; the targeted v008 suite passed.
5. Added the daily register assertion before adding the record; it failed with zero matching `svg / 2026-09-10` records.
6. Added the record and generated canonical page; the targeted v008 suite passed.

## Automated evidence

- `node --test tests/pure-svg-v008.test.mjs`: **4/4 passed**.
- `npm run test`: **194/194 passed; 0 failed, 0 skipped, 0 todo**.
- `node --check studies/pure-svg/v008/sketch.js`: **passed**.
- `node --check studies/pure-svg/v008/engine.mjs`: **passed**.
- `node --check tests/pure-svg-v008.test.mjs`: **passed**.
- `git diff --check`: **passed**.

## Headless browser evidence

Probe: `research/qa/proofs/svg-v008-2026-09-10/probe.mjs`
Result: `results.json`, **passed**, zero failures.

- Canonical route rendered the embedded v008 tableau before the heading at all five target viewports in normal and reduced-motion modes: **10/10**.
- At every run, requested width equaled `innerWidth`, `clientWidth`, and top-level `scrollWidth`; embedded frame `clientWidth` equaled `scrollWidth`.
- All three interaction buttons measured **44 CSS px** high at every matrix run.
- Initial canonical runs had empty console message buffers, zero page errors, zero request failures, and zero HTTP 400+ responses.
- Raw interactive preview: pointer changed memory `0 → 1` and produced three relayed routes; Enter on the focused SVG changed memory `1 → 2`; `unlink latest` returned memory `2 → 1` and restored the exact post-pointer body/routes/feet signature; `release the body` returned memory to `0`.
- Static reduced-motion preview at `390×844` kept the SVG visible, hid controls/readout/relay witnesses, and had no overflow.
- Evidence includes **13 PNG captures** plus the probe and result JSON.

## Critique and decision

The formal, perceptual, interaction, and cynical critiques are recorded in `critiques.json`. The technical gate is green. The independent caption-free perceptual comparison has not been performed by an independent observer, so this remains an honest **candidate / held** and is not called exhibition-ready.

No production deployment or GitHub publication is claimed in this record yet.
