# WebGPU v004 — 2026-09-08 QA record

## State

- Current: WebGPU
- Work: `webgpu-2026-09-08`
- Study: `/studies/webgpu/v004/`
- Status: `candidate / held`
- Canonical route: `/works/webgpu-2026-09-08/`

## Artistic rule

The previous vacancy rule is replaced by delayed rehearsal: a remembered crossing stores a signed gesture, then later agents compress into a phase-matched echo cohort and release a displaced copy of the earlier local order. The archive is carried by the crowd geometry, not by the witness ring or label.

## Structural evidence

- Seed: `0x57475034`
- 12 deterministic stages, 432 agents, memory limit 4
- Final memory: 4 rehearsals
- Final echo population: 145 agents
- Final rehearsal cohesion: `0.40053615502651946`
- Latest-lift field delta: `3.2878761593350068`
- Exact engine replay after lifting latest rehearsal: true
- Targeted test: 5/5 passed
- Full `npm test`: 144/144 passed
- `node --check`: `sketch.js` and `engine.mjs` passed
- `git diff --check`: passed

## Headless route and interaction evidence

Google Chrome headless served the canonical route and observed the data-driven work detail, embedded tableau, Journal anchor, critique list, evidence region, four-day WebGPU timeline, and previous-day link. Direct raw study navigation resolved to the canonical work. Explicit `preview=1&interaction=1&static=1` stayed raw, retained canvas and controls, and had no catalogue mount.

A same-origin headless probe exercised pointer rehearsal, Enter rehearsal, lift latest, and release crowd. The observed memory sequence was `0 → 1 → 2 → 1 → 0`; the status text changed to paused/lifted states as expected. Controls measured 44 CSS px high.

The isolated blind-mode captures are stored under `research/qa/proofs/webgpu-v004/`. All ten requested PNG dimensions were produced and nonblank. Valid DOM viewport runs passed at 768×1024, 1280×800, and 1920×1080 in normal and reduced-motion modes with `scrollWidth === clientWidth`; Chrome headless clamped 320px and 390px requests to a 500px CSS viewport, so those two DOM runs remain held rather than being treated as valid.

The first-party local route sweep returned HTTP 200 for 18 checked routes/assets. Browser-console capture was unavailable in this run; Chrome stderr contained external GCM registration/authentication noise but no page error in the route dumps.

## Production verification

After commit `38fd2e6736aa8f988ac08c9e4aef13c75ca159b1`, GitHub `origin/main` matched the local SHA. Vercel deployment `dpl_C89Fz1avoxPYuhmkxnVZTtzn4EUo` reached `READY` at `https://autopoiesis-ow9k6bf71-lairpa-hotmailfrs-projects.vercel.app` and was aliased to `https://autopoiesis-nine.vercel.app`. The stable register returned HTTP 200 with the target record. A rendered headless read of `/journal/` found the target anchor and title; a rendered read of `/works/webgpu-2026-09-08/` found the ready mount, v004 tableau iframe, and Journal link.

## Critique and unresolved doubt

The structural rule is real and reversible, but the work remains held until an independent caption-free perceptual comparison confirms that the repeated cohort rhythm survives with echo witnesses, route threads, labels, and readout hidden. The narrow DOM viewport runs and browser-console probe also remain unresolved.
