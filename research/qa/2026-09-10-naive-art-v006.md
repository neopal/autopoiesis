# Naive art v006 daily studio record — 2026-09-10

## Scope and candidate

This scheduled rotation targeted Naive art (`naive`) on `2026-09-10`; the pre-run slot was empty. The canonical sources were `studio/data/studio.json` (`mutine-studio/v2`) and `studio/data/works.json` (`mutine-works/v1`). Field tests and stimuli were not treated as work records. The missed `2026-09-09` Naive slot remains absent.

A new candidate tableau was implemented at `/studies/naive-art/v006/` and recorded exactly once as `naive-2026-09-10`. The record is `candidate / held`; no promotion to complete is claimed.

**Title:** The mistake comes back.

- **Changed rule:** v005 made each retained correction a shared hinge. v006 abandons the single-turn rule: every retained correction becomes an advance-return-continue reversal, replayed from the house edge, ground, and route.
- **Visible consequence:** the refused panel stays direct; the kept panel repeats the same small return rhythm across the house edge, ground, and path.
- **Deterministic state:** seed `0x4e413036`, eleven stages, 34-shape budget, ten autonomous memory records with an eight-record structural window at the settled state, deterministic SVG path geometry.
- **Interaction:** pointer/tap on the field or the `BRING IT BACK` button adds a bounded visitor reversal; Enter/Space follows the keyboard path; `UNDO LAST` rebuilds the exact preceding field; `RELEASE SEQUENCE` returns to the seeded sequence.
- **Falsifier:** hide labels, readout, bridge, sequence marks, and reversal dots. If the kept marks no longer show repeated returns across house and path, the rule is ornamental.
- **Deletion condition:** delete v006 if two caption-free comparisons cannot identify the repeated return rhythm, or if it reads as an ordinary zigzag.

## Changed files

- `studies/naive-art/v006/README.md`
- `studies/naive-art/v006/critiques.json`
- `studies/naive-art/v006/engine.mjs`
- `studies/naive-art/v006/index.html`
- `studies/naive-art/v006/metrics.json`
- `studies/naive-art/v006/sketch.js`
- `studies/naive-art/v006/style.css`
- `tests/naive-art-v006.test.mjs`
- `studio/data/works.json`
- `works/naive-2026-09-10/index.html` (canonical generated shell)
- `tests/catalog-architecture.test.mjs`, `tests/daily-catalog.test.mjs`, `tests/evolution-catalog.test.mjs` (updated register-count/order expectations from 38 to 39 for this real daily record)
- `research/qa/proofs/naive-art-v006-2026-09-10/probe.cjs`
- `research/qa/proofs/naive-art-v006-2026-09-10/results.json`
- `research/qa/proofs/naive-art-v006-2026-09-10/*.png` (thirteen local captures)
- `research/qa/2026-09-10-naive-art-v006.md`

## Repository and structural evidence

- `studio/data/studio.json`: PASS — valid JSON; six registered currents.
- `studio/data/works.json`: PASS — valid JSON; 39 records; target appears exactly once.
- Current/date uniqueness: PASS — no duplicate `currentId/date` pairs; `naive/2026-09-09` remains absent.
- Target raw tableau: PASS — v006 index, engine, sketch, style, metrics, critiques, and README exist.
- Target canonical page: PASS — `works/naive-2026-09-10/index.html` exists and carries `data-catalog-work-detail="naive-2026-09-10"`.
- Target Journal relation: PASS — `journal-naive-2026-09-10`; canonical shell points to `/journal/#journal-naive-2026-09-10`.
- Critique condition: PASS — four named critiques are recorded; status remains held.
- Lineage: PASS — `naive-2026-09-08` → v006; no fabricated 2026-09-09 work was added.

## TDD and automated verification

- **RED:** `node --test tests/naive-art-v006.test.mjs` initially failed with `ERR_MODULE_NOT_FOUND` because v006 did not yet exist.
- **RED:** the focused browser-surface test then failed with `ENOENT` because v006’s index/controls did not yet exist.
- **RED:** `npm test` exposed the expected 38→39 register-count/order regressions after the real record was added.
- **GREEN:** `node --test tests/naive-art-v006.test.mjs`: **PASS — 4/4**.
- **GREEN:** targeted catalog tests: **PASS — 16/16**.
- **Full suite:** **PASS — 204/204 tests, 0 failed, 0 skipped, 0 todo**.
- **JavaScript syntax:** **PASS** — `node --check studies/naive-art/v006/engine.mjs`, `node --check studies/naive-art/v006/sketch.js`, and `node --check research/qa/proofs/naive-art-v006-2026-09-10/probe.cjs`.
- **Whitespace:** **PASS** — `git diff --check`.
- **Added-line security scan:** no matches for hardcoded-secret, shell-injection, eval/exec, unsafe-deserialization, or SQL-construction patterns in the changed source.
- **Independent reviewer:** unavailable in this scheduled runtime; no independent approval is claimed.

## Local headless browser evidence

The local server was `python -m http.server 4173 --bind 127.0.0.1`. The route was exercised with Playwright and the installed Chrome executable in headless mode only; no GUI, real profile, CDP, remote debugging, or CUA was used.

Canonical route:

`http://127.0.0.1:4173/works/naive-2026-09-10/`

Raw routes:

- `http://127.0.0.1:4173/studies/naive-art/v006/?preview=1&interaction=1`
- `http://127.0.0.1:4173/studies/naive-art/v006/?preview=1&static=1`

Saved evidence: `research/qa/proofs/naive-art-v006-2026-09-10/results.json`, `probe.cjs`, and thirteen PNG captures.

- Canonical shell and embedded v006 tableau: **10/10 runs rendered**.
- Viewport matrix: **10/10 passed** — `320x568`, `390x844`, `768x1024`, `1280x800`, and `1920x1080`, each in normal and reduced-motion modes.
- Probe validity and overflow: every canonical run reported `innerWidth === clientWidth === scrollWidth`; raw interactive and blind 390x844 runs matched as well.
- Tableau ordering: canonical `studio-main` mounted the shell path, then the generated detail mount rendered the full-artwork iframe before title, journal, critique, evidence, and neighbouring-work prose.
- Preview mode: embedded raw tableau reported `preview-mode` and `interactive-preview`.
- Reduced motion: canonical reduced runs settled at `stage 11 / 11` with `8 reversals retained`; normal runs opened at `stage 04 / 11` with `3 reversals retained`, making the rule visible immediately.
- Touch targets: all three controls measured exactly 44 CSS px high at every matrix run and at raw 390x844.
- Initial console/network: **0 console messages, 0 page errors, 0 failed requests, 0 first-party responses >=400** across canonical, raw interactive, and blind runs.
- Interaction: pointer on the field changed `3`→`4` reversals and paused; Enter changed `4`→`5` with further route geometry; undo returned `5`→`4` and exactly restored the pointer-state route path; release returned to `stage 01 / 11`, `0 reversals retained`, and the initial plain route.
- Caption-free blind preview: reduced-motion raw `390x844` capture hid labels, readout, bridge, sequence marks, controls, and reversal dots; the field remained visible and overflow-free. This is capture evidence, not an independent perceptual verdict.

## Artistic doubt and release boundary

The structural, automated, and local browser gates are green. The candidate remains **held** because no independent caption-free perceptual comparison has yet been observed by a separate critic with the witness layer hidden. The key doubt is whether the repeated advance/return/continue rhythm reads as one remembered naive gesture rather than an attractive generic zigzag. Public production visibility, GitHub synchronization, and deployed Journal verification remain pending the authorized release step.

**Archive decision:** record the candidate; do not call it exhibition-ready.
