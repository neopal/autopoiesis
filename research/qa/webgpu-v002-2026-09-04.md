# WebGPU v002 / 2026-09-04

## Candidate record

- Current: `webgpu`
- Work: `webgpu-2026-09-04`
- Canonical path: `/works/webgpu-2026-09-04/`
- Raw tableau: `/studies/webgpu/v002/`
- Status: candidate / held
- Rule: an archived point becomes latency debt; later agents change phase and arrive in staggered bands instead of one shared cadence.
- Seed: `0x57475032`
- Interaction: pointer capture, Enter/Space commit, Delete/Backspace lift, release crowd; `blind=1` hides witnesses/readouts for critique.

## Files

- `studies/webgpu/v002/index.html`
- `studies/webgpu/v002/sketch.js`
- `studies/webgpu/v002/engine.mjs`
- `studies/webgpu/v002/style.css`
- `studies/webgpu/v002/README.md`
- `studies/webgpu/v002/metrics.json`
- `studies/webgpu/v002/critiques.json`
- `tests/webgpu-v002.test.mjs`
- `studio/data/works.json`
- generated `works/webgpu-2026-09-04/index.html`

## Structural evidence

- Strict TDD cycle observed: the new v002 test first failed with `ERR_MODULE_NOT_FOUND`; the engine was added, the visitor edge case then failed as expected, and the minimal engine fix made the slice pass.
- v002 test file: **6/6 passed**.
- Full suite: **97/97 passed**, 0 failed, 0 skipped, 0 todo.
- `node --check studies/webgpu/v002/sketch.js`: **PASS**.
- `node --check studies/webgpu/v002/engine.mjs`: **PASS**.
- `git diff --check`: **PASS**; Git emitted only expected LF→CRLF working-copy warnings.
- Target HTTP checks on the local server: canonical, raw preview, engine, and sketch all returned **200**.
- Register check: 20 works, one `webgpu/2026-09-04` slot, zero duplicate current/date slots; raw and canonical files exist.
- Target security-pattern scan: **0 findings** for secret assignment, shell injection, eval/exec, unsafe deserialization, or SQL-injection patterns.

## Browser evidence actually observed

- Local canonical URL loaded: `http://localhost:4173/works/webgpu-2026-09-04/`; title and nested interactive iframe were present.
- Accessibility snapshot exposed the WebGPU artwork region, canvas image label, and the three 44px action controls.
- One initial browser console probe returned **0 console messages / 0 JS errors**.
- After a control click, the post-interaction canvas probe found a real `714×720` canvas with `514080` non-black pixels and the expected `preview-mode interactive-preview` classes. This proves raster output exists in the browser context.
- The headless visual screenshot displayed the canvas as black; the existing v001 canvas displayed the same way in the same probe, so the visualizer did not provide reliable Canvas raster evidence.
- A repeated accessibility snapshot triggered the browser `idempotent_no_progress` guardrail. Browser probing stopped immediately as required.

## Unresolved / held

- The browser click did not yield a conclusive `visitor delay / paused` accessibility readout before the guardrail; interaction-state confirmation is held.
- The five-viewport normal/reduced-motion matrix (`320×568`, `390×844`, `768×1024`, `1280×800`, `1920×1080`) was not observed.
- Raw-tableau navigation through `browser_navigate` hit a UTF-8 decoding error; local HTTP requests to the same raw preview returned 200.
- No public deployment was performed or claimed; the deployed route remains unverified.
- No independent caption-free perceptual review was obtained. The deletion condition remains active: delete v002 if staggered arrival is not legible with debt witnesses/readouts hidden, or if timing threads carry the meaning alone.

## Decision

Record the real tableau and canonical daily page, but keep `webgpu-2026-09-04` **candidate / held**. No commit, push, or deploy was performed.
