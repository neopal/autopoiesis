# WebGPU v010 QA — 2026-09-15

## Decision

**Candidate / held.** The v010 tableau is real, deterministic, structurally reversible, and locally browser-verified. It is not marked exhibition-ready: independent caption-free perceptual review and provider revision linkage remain unresolved.

## Artistic contract

- **Hypothesis:** a crowd becomes an archive when a remembered absence changes the order of who arrives next.
- **Changed rule:** v009 reversed a bounded packet against the main flow; v010 makes neighboring cohorts exchange lanes through a finite crossing, then retain the exchanged order downstream.
- **Visible consequence:** the agent coordinates carry entry, crossing, and settled handoff offsets; the witness route is not the only carrier of the rule.
- **Interaction:** pointer or Enter/Space creates the same bounded lane switch; Delete/Backspace or lift latest rebuilds the exact prior field; release current restarts the seeded sequence.
- **Falsifier/deletion:** delete v010 if crossing plus changed downstream order is not legible with witnesses, labels, and readout hidden, or if the action changes only decoration.

## Deterministic engine evidence

`studies/webgpu/v010/engine.mjs` reports 18 stages, 480 agents, memory limit 7, final memory 7, 249 crossing agents, 170 switched agents, 232 settled agents, switch strength `0.04622637170922601`, latest cross load `4.158425373039698`, latest settle load `6.843938907739637`, and latest lifted field delta `2.9002623868148483`. The visitor point `{x:1.4,y:-0.2}` is bounded to `{x:0.92,y:0.08}` and produces cross load `2.0093925985519507`.

## Local headless browser evidence

Source: `research/qa/proofs/webgpu-v010-2026-09-15/results.json` and `probe.mjs`, using Python HTTP server on port 4173 and Playwright + Google Chrome headless.

- Canonical route: `/works/webgpu-2026-09-15/` rendered the v010 iframe tableau before generated title, Journal, critique, evidence, and neighbouring-work prose.
- Five viewports × normal/reduced motion: **10/10 passed** at 320×568, 390×844, 768×1024, 1280×800, and 1920×1080.
- Overflow: outer and embedded `innerWidth`, `clientWidth`, and `scrollWidth` matched in all ten matrix runs; no out-of-bounds shell offenders were reported.
- Runtime: zero console messages, page errors, failed requests, and HTTP 400+ responses across canonical, raw interaction, static-blind, and Journal runs.
- Raw interaction at 390×844: pointer 0→1 switch, changed the canvas signature, Delete 1→0 with exact signature restoration, Enter 0→1, Delete 1→0, release returned to stage 01 / 18 with 0 switches.
- Controls: three raw controls measured at least 44 CSS px.
- Static blind preview: canvas remained visible while controls and readout were hidden; no overflow.
- Direct raw path redirected to `/works/webgpu-2026-09-15/`.
- Journal readback rendered exactly one `#journal-webgpu-2026-09-15` entry with the recorded title and canonical Journal href.
- Favicon returned HTTP 200 as `image/svg+xml`.
- 13 PNG captures were written: 10 matrix captures, raw interaction, static blind, and Journal.

## Unresolved

1. Independent caption-free perceptual comparison with switch witnesses, labels, and readout hidden.
2. Provider revision linking a future stable-alias deployment to the eventual GitHub SHA.
